# Completadas 2026-09-08 (II) — `content_search` integrado + división `tools_archivo.rs`

## 089A-6 — Búsqueda indexada v1 (superada, referencia histórica)

- Spike Fase 0 con `C:\tmp\tgrep\tgrep.exe` (v1.0.5, asset win-x64 oficial):
  índice glory-harness 269 ms / queries 85–1108 ms; criterio p50 < 500 ms cumplido.
- Tool v1 por `GLORY_TGREP_BIN` + fallback local, registrada con `CAT_LECTURA`.
- Gate 089A-6 PASS con 1 warning (`limite-lineas`: `tools_archivo.rs` 1342 líneas).
- **Superada por 089A-8** (directiva: integración 100% sin depender del sistema).
  Código v1 eliminado. Plan: `Agente/planes/completados/plan-089A-6-busqueda-indexada-2026-09-08.md`.

## 089A-7 — Dividir `tools_archivo.rs` (HECHO como parte de 089A-8)

- Qué: `ToolContentSearch` extraída a `core/src/herramientas/archivo/content_search.rs`;
  `tools_archivo.rs` movido a `core/src/herramientas/archivo/tools_archivo.rs`
  (1342→~830 líneas); `herramientas/` conserva 10 ficheros vía subdirectorio
  `archivo/` con `pub use` (rutas `crate::content_search::*` y
  `crate::tools_archivo::*` intactas; precedente: `navegador/`).
- Deuda `limite-lineas` del gate 089A-6 cerrada; gate 089A-8 sin ese aviso.

## 089A-8 — `content_search` integrado 100% (HECHO)

- Qué: motor in-process con `tgrep-core` (git tag v1.0.5; `Cargo.lock` pinnea
  `d55b0220...`; verificado que NO está en crates.io — 404). Sin binario externo
  ni `GLORY_TGREP_BIN`.
- Archivos:
  - `core/Cargo.toml`: deps `regex` + `tgrep-core`; `Cargo.lock` actualizado.
  - `core/src/herramientas/archivo/content_search.rs` (nuevo): tool + motor + 6 tests.
  - `core/src/herramientas/archivo/mod.rs` (nuevo), `herramientas/mod.rs`
    (re-export), `herramientas/tool.rs` (descripción en el catálogo),
    `nucleo/subagente.rs` + `politica/regla.rs` (vienen de la v1: whitelists y
    `CAT_LECTURA`).
- Diseño: índice persistente por workspace en
  `<temp>/glory-harness/tgrep-idx/<hash16>/` + `glory-manifest.json`
  (foto ruta/mtime/tamaño del mismo paseo del builder; rebuild si difiere);
  `Mutex` por índice; `spawn_blocking` + timeout 180 s; verificación con
  `regex` + `sandbox.leer` (hereda contención y lista negra de secretos);
  smart-case; `glob`/`limite`/`solo_archivos`; salida `ruta:linea:contenido`
  máx ~12 KB + `[truncado]`; fallback local con aviso (nunca falla).
  Sin `unsafe` (`#![forbid(unsafe_code)]` intacto).
- Evidencia:
  - `cargo test -p glory-harness-core --lib`: **263 passed, 0 failed**.
  - `cargo clippy -p glory-harness-core --all-targets`: sin warnings en
    los ficheros tocados. `rustc` emite 16 warnings, todos pre-existentes en
    `herramientas/navegador/reflejo.rs` (escaped-newline, ajeno a este cambio).
  - Gate `sentinel check 089A-8`: **PASS, 0 errores, 0 warnings, 0 info**
    (reporte `.quality-reports/check/089A-8/latest.md`). En el camino se
    corrigieron `directorio-abarrotado` (subdirectorio `archivo/`),
    `funcion-larga-rs` (`bloquear_indice` extraída de `ejecutar`) y 2
    `todo-pendiente` (comentario + fixture de test).
  - `rustfmt --edition 2021` limpio en los 4 ficheros tocados
    (`tool.rs` tiene 1 diff pre-existente en imports, no tocado).
- Gotchas:
  - `C:` llegó a 0 bytes libres durante la sesión (límite `C:\tmp` 7 GB
    superado: target 7.3 GB); se liberaron 2.75 GB borrando
    `debug/incremental` (2.25 GB) e índices del spike `idx-area`/`idx-gh`
    (~500 MB, evidencia ya registrada) + fuentes tgrep. Fijar vigilancia.
  - `tgrep::query` permite `*` inicial sin error pero no lo ejecuta como
    comodín real; la verificación `regex` manda (documentado en la tool).
  - El check `todo-pendiente` salta con la palabra "Todo" en comentarios y
    fixtures ("Todo síncrono", `"// TODO:"` en un seed): redactar neutral.
- Pendientes reales: ninguno de 089A-8. La UI 089A-3/4 sigue pendiente de
  verificación visual (089A-5).
