# Tareas completadas — 2026-09-10

## 109A-1 — Hook pre-compactación (HECHO)

- **Qué se hizo:**
  - Se completó `ContextoConfig.gancho_pre_compact` y el dispatcher previo a la compactación.
  - El runner de comando externo aplica un timeout único a spawn, escritura, espera y drenaje; inicia el lector de stdout antes de escribir stdin; limita stdout a 64 KiB; interpreta `exit 2` como veto y valida estrictamente los ajustes JSON (`resumen`/`resumen_llm`).
  - Los fallos del hook, timeouts, stdout inválido y JSON inválido se observan como warning y no bloquean la compactación.
  - El runner HTTP no expone URL completa, credenciales, query, fragmento ni ruta en logs/errores; solo registra `esquema://host:puerto` para respuestas no-2xx.
  - La configuración persistida usa un resolver compartido con warning explícito cuando el JSON es inválido. Se aplica a sesión, `run`, `chat`, `tui` y `schedule run`, manteniendo overrides explícitos de proveedor/modelo. En `run`, la ausencia de directorio durable no es error; un fallo real de apertura/lectura sí se propaga. El borrado/null de la UI queda consistente.
  - Revisión independiente aplicada: `exit 1` y cualquier código no cero distinto de `2` quedan observables como warning (antes eran silenciosos) y `run` carga el hook persistido cuando existe almacenamiento durable.
- **Archivos principales:**
  - `core/src/nucleo/hooks.rs`
  - `core/src/nucleo/context.rs`
  - `core/src/nucleo/runtime/mod.rs`
  - `core/src/nucleo/runtime/turno/mod.rs`
  - `cli/src/servicio/sesion_config.rs`
  - `cli/src/servicio/sesion.rs`
  - `cli/src/servicio/mod.rs`
  - `cli/src/comandos/run.rs`
  - `cli/src/schedule_cmd.rs`
  - `desktop/ui/src/adaptadores/api.ts`
  - además de los archivos de integración ya modificados en el bloque.
- **Evidencia local:**
  - `cargo test -p glory-harness-core --lib --quiet`: **273 passed, 0 failed**.
  - `cargo test -p glory-harness --lib --quiet`: **102 passed, 0 failed**.
  - `cargo clippy -p glory-harness-core -p glory-harness --all-targets -- -D warnings`: limpio.
  - `npm.cmd --prefix desktop/ui run build`: TypeScript y Vite correctos; 85 módulos transformados.
  - `git diff --check`: correcto.
  - Disco temporal: `C:\tmp` por debajo del límite de 7 GB.
  - Sentinel reducido sobre el alcance propio (18 archivos): versión 0.7.8, **0 errores / 0 warnings / 0 info / 0 hints** (`.quality-reports/analyze-109a1.json`).
- **Gate canónico:**
  - Ejecutado con `sentinel check 109A-1 --stages scripts/quality/stages.json` tras los cambios.
  - Resultado honesto: **FAIL**. `coverage` pasa; `sccache` falla por `sccache-no-configurado`; Sentinel encuentra 317 errores, 206 warnings y 39 infos principalmente en `data/referencias-cli/**`, incluido por el patrón global `**/*.rs`.
  - Esos hallazgos son deuda heredada fuera del alcance funcional de 109A-1 y no se mezclaron ni ocultaron. No se declara PASS global.
- **Decisiones/gotchas:**
  - El parser compartido conserva la semántica de no bloqueo: un JSON persistido inválido se ignora con warning, no silenciosamente.
  - El lease local de calidad se emitió para las pruebas Rust; no se hizo push, deploy, SSH ni escritura externa.
  - `scripts/quality/stages.json` y `scripts/quality/sentinel-sccache.mjs` permanecen fuera del commit de esta tarea por ser cambios ajenos/preexistentes.
  - Alcance explícito: la configuración durable del hook cubre el runner de comando. El `RunnerComandoHttp` queda como API interna del núcleo (sin ruta de configuración persistida); si se necesita, es tarea propia.
  - Definición de `exit 2` como veto y `!= 0` como warning está cubierta por pruebas reales de PowerShell en Windows.
- **Pendientes reales:**
  - El timeout acota el ciclo completo del hijo, pero no hay limpieza explícita de descendientes del comando en Windows (gotcha heredado de `tokio::process`).
  - El gate global sigue rojo por dos causas ajenas a este bloque (`sccache-no-configurado` y deuda de `data/referencias-cli/**`, que entra por el patrón global `**/*.rs` del analizador); registrado como tarea independiente en el roadmap.
- **Siguiente:** continuar con `109A-2` (memoria estrictamente por proyecto y export/import); no iniciar F3 hasta cerrar su base de persistencia.

## 109A-2 — Memoria estrictamente por proyecto + export/import (HECHO)

- **Qué se hizo:**
  - Contrato del núcleo: enum `AmbitoMemoria { Global | Proyecto(Uuid) }` con `desde_proyecto`,
    `proyecto_id`, `etiqueta`; `AgentPersistence::memoria_listar/upsert/borrar` reciben el ámbito y
    se añade `memoria_ambitos` (método por defecto) que enumera los ámbitos con contenido más el
    global. `AgentToolContext.ambito_memoria` y `TurnoConfig.ambito_memoria` lo propagan a las tools,
    al proveedor (`MemoriaBase::nuevo(..., ambito)`, `marcar_uso(..., ambito)`) y al curador.
  - Curador: `ejecutar_curador_todos` recorre todos los ámbitos (el cron ya no deja sin curar los
    recuerdos de proyecto) y anota ` [proyecto {uuid}]` en las claves del reporte para no confundir
    recuerdos homónimos de proyectos distintos.
  - Migración SQLite: SQLite no permite alterar una `PRIMARY KEY`, así que `abrir_conexion` renombra
    `memoria` a `memoria_pre_109a2`, crea la tabla nueva con
    `workspace_id TEXT NOT NULL DEFAULT ''` y `UNIQUE (user_id, workspace_id, clave)`, copia las
    filas como globales, elimina la vieja y confirma todo en una transacción. Es idempotente: detecta
    la columna con `PRAGMA table_info` y no repite el trabajo al reabrir.
  - CLI: `ambito_de_ruta` resuelve el área de la carpeta activa (canoniza y quita el prefijo verbatim
    de Windows) y degrada a global con aviso si no hay área o falla la consulta; `memoria` acepta
    `--global`, `--proyecto <uuid|ruta>` y `listar --todos`, y un `--proyecto` inexistente es error
    explícito (nunca un fallback silencioso a global).
  - Export/import markdown por recuerdo en `cli/src/infra/memoria_io.rs`: frontmatter
    `clave/origen/usos/ultimo_uso/actualizada_en` y cuerpo; destino `local` (carpeta de datos de la
    app) o `project` (`.glory/memorias` del área, versionable en el repo). Reexportar reutiliza el
    archivo del mismo recuerdo y desambigua el nombre con sufijo si el slug colisiona. El import es
    frontera de entrada no confiable: `sanitize_para_memoria` se aplica a clave y contenido, y un
    archivo que parece credencial se omite con motivo sin guardar a medias.
- **Archivos principales:**
  - `core/src/contrato/ports.rs`, `core/src/lib.rs`, `core/src/herramientas/tool.rs`
  - `core/src/nucleo/runtime/mod.rs`, `core/src/nucleo/runtime/tools.rs`
  - `core/src/nucleo/memoria/{curador,proveedor,soporte,tools}.rs`
  - `cli/src/persistencia_sqlite.rs`, `cli/src/persistencia_sqlite/{puerto,memoria}.rs`
  - `cli/src/comandos/{memoria,run}.rs`, `cli/src/infra/{memoria_io,persistencia}.rs`
  - `cli/src/ui/chat.rs`, `cli/src/ui/tui/bucle.rs`
  - mocks y contextos de test adaptados en `core/src/**`
- **Evidencia local:**
  - `cargo test -p glory-harness-core --lib` → **117 passed, 0 failed**.
  - `cargo test -p glory-harness --lib` → **276 passed, 0 failed**.
  - `cargo clippy -p glory-harness-core -p glory-harness --all-targets -- -D warnings`: limpio.
  - `npm.cmd --prefix desktop/ui run build`: TypeScript y Vite correctos (85 módulos).
  - `git diff --check`: limpio.
  - Aislamiento y migración cubiertos con pruebas reales:
    `memoria_aislada_entre_global_y_proyectos`, `upsert_repite_clave_solo_en_su_ambito`,
    `memoria_no_cruza_usuarios`, `migra_memoria_legacy_al_ambito_global_sin_perder_datos`,
    `prefetch_no_mezcla_ambitos`, `sync_escribe_solo_en_su_ambito`,
    `curador_todos_cura_cada_ambito_por_separado`, `export_import_ida_y_vuelta_por_archivos`,
    `destino_export_*` y `ambito_pedido_rechaza_proyecto_inexistente`.
- **Gate canónico:**
  - `sentinel check 109A-2 --stages scripts/quality/stages.json` → **FAIL** honesto:
    `coverage` PASS 0/0/0; `sccache` FAIL (1 error, configuración de sccache, etapa apoyada en
    `scripts/quality/sentinel-sccache.mjs`, archivo ajeno sin versionar); etapa `sentinel` FAIL con
    317 errores / 206 warnings, todos en `data/referencias-cli/**` (deuda heredada, ya documentada en
    109A-1) y 10 warnings preexistentes en `desktop/ui/**` ajenos a este cambio.
  - **0 hallazgos en `cli/src` y `core/src`** (alcance real de 109A-2) según el JSON del reporte.
  - No se declara PASS global; la deuda ajena queda como tarea independiente en el roadmap.
- **Decisiones/gotchas:**
  - `workspace_id` usa `''` como centinela de “global” en vez de `NULL`: en SQLite los `NULL` no
    colisionan en una restricción `UNIQUE`, así que el `ON CONFLICT (user_id, workspace_id, clave)`
    del upsert dejaría de ser fiable y se duplicarían filas.
  - La migración recrea la tabla porque la PK original (`user_id, clave`) no se puede alterar; se hace
    en una sola transacción para que un fallo no deje la memoria a medias.
  - `memoria_ambitos` devuelve los ámbitos **con contenido** más el global (siempre), así que un
    proyecto cuyos recuerdos se borran deja de aparecer; no implica pérdida de datos.
  - `run` usa persistencia efímera, por lo que no resuelve área y trabaja siempre en ámbito global
    (comentado en el código).
  - El módulo de markdown vive en `cli/src/infra/` y no en `cli/src/comandos/` para respetar el
    límite de 10 archivos por directorio que aplica VarSense (el primer gate lo marcó como
    `directorio-abarrotado` y dejó de aparecer tras el movimiento).
  - `scripts/quality/stages.json` y `scripts/quality/sentinel-sccache.mjs` permanecen fuera del commit
    por ser cambios ajenos/preexistentes.
- **Pendientes reales:**
  - El binario CLI no se pudo recompilar para la comprobación manual (`glory-harness.exe` en
    ejecución bloqueaba la escritura del enlace); en su lugar se probaron las funciones reales del
    subcomando (resolución de destino, ámbito pedido e ida y vuelta de archivos). Reintentar la
    prueba manual cuando no haya proceso en ejecución.
  - `C:\tmp` quedó por encima del techo de 7 GB durante la sesión (targets de `glory-harness` y
    `coolify-manager`); lo gestiona la tarea programada de limpieza, no se borraron targets ajenos.
  - El escritorio (`desktop/`) todavía no consume el ámbito de memoria; el wiring de UI corresponde a
    F3 (109A-3).
- **Siguiente:** `109A-3` (sección “Memorias” en Configuración, panel custom + IPC Tauri), después
  `109A-4` F2-F4 y `109A-5` F2.
