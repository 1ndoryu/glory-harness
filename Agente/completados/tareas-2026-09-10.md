# Tareas completadas — 2026-09-10

## 109A-1 — Hook pre-compactación (HECHO)

- **Qué se hizo:**
  - Se completó `ContextoConfig.gancho_pre_compact` y el dispatcher previo a la compactación.
  - El runner de comando externo aplica un timeout único a spawn, escritura, espera y drenaje; inicia el lector de stdout antes de escribir stdin; limita stdout a 64 KiB; interpreta `exit 2` como veto y valida estrictamente los ajustes JSON (`resumen`/`resumen_llm`).
  - Los fallos del hook, timeouts, stdout inválido y JSON inválido se observan como warning y no bloquean la compactación.
  - El runner HTTP no expone URL completa, credenciales, query, fragmento ni ruta en logs/errores; solo registra `esquema://host:puerto` para respuestas no-2xx.
  - La configuración persistida usa un resolver compartido con warning explícito cuando el JSON es inválido. Se aplica a sesión, `run`, `chat`, `tui` y `schedule run`, manteniendo overrides explícitos de proveedor/modelo. En `run`, la ausencia de directorio durable no es error; un fallo real de apertura/lectura sí se propaga. El borrado/null de la UI queda consistente.
  - Revisión independiente aplicada: `exit 1` y cualquier código no cero distinto de `2` quedan observables como warning (antes eran silenciosos) y `run` carga el hook persistido cuando existe almacenamiento durable.
- **Archivos principales:**
  - `core/src/nucleo/hooks.rs`
  - `core/src/nucleo/context.rs`
  - `core/src/nucleo/runtime/mod.rs`
  - `core/src/nucleo/runtime/turno/mod.rs`
  - `cli/src/servicio/sesion_config.rs`
  - `cli/src/servicio/sesion.rs`
  - `cli/src/servicio/mod.rs`
  - `cli/src/comandos/run.rs`
  - `cli/src/schedule_cmd.rs`
  - `desktop/ui/src/adaptadores/api.ts`
  - además de los archivos de integración ya modificados en el bloque.
- **Evidencia local:**
  - `cargo test -p glory-harness-core --lib --quiet`: **273 passed, 0 failed**.
  - `cargo test -p glory-harness --lib --quiet`: **102 passed, 0 failed**.
  - `cargo clippy -p glory-harness-core -p glory-harness --all-targets -- -D warnings`: limpio.
  - `npm.cmd --prefix desktop/ui run build`: TypeScript y Vite correctos; 85 módulos transformados.
  - `git diff --check`: correcto.
  - Disco temporal: `C:\tmp` por debajo del límite de 7 GB.
  - Sentinel reducido sobre el alcance propio (18 archivos): versión 0.7.8, **0 errores / 0 warnings / 0 info / 0 hints** (`.quality-reports/analyze-109a1.json`).
- **Gate canónico:**
  - Ejecutado con `sentinel check 109A-1 --stages scripts/quality/stages.json` tras los cambios.
  - Resultado honesto: **FAIL**. `coverage` pasa; `sccache` falla por `sccache-no-configurado`; Sentinel encuentra 317 errores, 206 warnings y 39 infos principalmente en `data/referencias-cli/**`, incluido por el patrón global `**/*.rs`.
  - Esos hallazgos son deuda heredada fuera del alcance funcional de 109A-1 y no se mezclaron ni ocultaron. No se declara PASS global.
- **Decisiones/gotchas:**
  - El parser compartido conserva la semántica de no bloqueo: un JSON persistido inválido se ignora con warning, no silenciosamente.
  - El lease local de calidad se emitió para las pruebas Rust; no se hizo push, deploy, SSH ni escritura externa.
  - `scripts/quality/stages.json` y `scripts/quality/sentinel-sccache.mjs` permanecen fuera del commit de esta tarea por ser cambios ajenos/preexistentes.
  - Alcance explícito: la configuración durable del hook cubre el runner de comando. El `RunnerComandoHttp` queda como API interna del núcleo (sin ruta de configuración persistida); si se necesita, es tarea propia.
  - Definición de `exit 2` como veto y `!= 0` como warning está cubierta por pruebas reales de PowerShell en Windows.
- **Pendientes reales:**
  - El timeout acota el ciclo completo del hijo, pero no hay limpieza explícita de descendientes del comando en Windows (gotcha heredado de `tokio::process`).
  - El gate global sigue rojo por dos causas ajenas a este bloque (`sccache-no-configurado` y deuda de `data/referencias-cli/**`, que entra por el patrón global `**/*.rs` del analizador); registrado como tarea independiente en el roadmap.
- **Siguiente:** continuar con `109A-2` (memoria estrictamente por proyecto y export/import); no iniciar F3 hasta cerrar su base de persistencia.
