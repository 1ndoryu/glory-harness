# 2026-09-17 - cierre 179A-1 (gate rápido + completo)

## 179A-1 Gate en dos niveles: rápido ≤60 s + completo de cierre
- Qué: `scripts/quality/stages-fast.json` (mismas coverage/sccache/sentinel,
  timeouts 60 s, etapa `rust --fast`) + modo fast en
  `scripts/quality/sentinel-rust.mjs` (tool 1.1.0, reporte `mode`):
  `cargo check --tests` + `cargo test --lib` filtrado por alcance (módulo
  como subcadena libtest), sin clippy; veredicto `rust-modo-rapido`
  informativo, no válido para cierre. Fail-closed con `rust-pide-completo`
  (error): sin alcance, desktop/manifiestos, contrato, >10 archivos,
  >5 filtros y alcance sin `.rs` bajo core|cli/src.
- Evidencia: F1 (binario real, target `C:\tmp\glory-target`): check core
  frío 140 s / caliente 1,2–1,5 s; test `turno` frío 157 s / caliente 1,3 s
  (8 passed, 328 filtrados) — el rápido solo cabe en 60 s en caliente.
  Fast directo 6,2 s (check 0 + 8 tests). Fail-closed probado en 4 casos
  (contrato, lockfile, 12 archivos, alcance sin `.rs`). Regresión completo
  directo: 305 s, clippy 0, 505 tests ok. Piloto orquestador: rápido 14,1 s
  (`rust-pide-completo` en 0,4 s); completo 373,8 s con etapa `rust` **pass**
  (362 s). Reportes `.quality-reports/check/179A-1/`.
- Bug encontrado por el piloto: alcance sin `.rs` derivaba 0 filtros y corría
  todo el lib hasta el timeout (78,8 s) — corregido a pide-completo.
- Cierre: veredicto global FAIL solo por 30 findings sentinel preexistentes
  en ficheros no tocados por esta tarea (dueño: **149A-2**, ya en roadmap);
  alcance propio verde (rust pass + docs). Sin backdoor en el guard
  (`cargo` directo sigue bloqueado; medición con binario real + token).
- Docs: `AGENTS.md` raíz §5 (subsección dos niveles) + skill
  `quality-gate-setup` (nota `stages-fast.json`); skill `sentinel` omitida
  (no menciona `sentinel check`). Plan:
  `Agente/planes/completados/plan-179A-1-gate-rapido-lento-2026-09-17.md`.
- Prevención/Sentinel: regla de decisión rápido-vs-completo queda en AGENTS
  §5; la deuda base (path-join, sqlite-N, funcion-larga, limite-lineas,
  god-object, large-interface) sigue en 149A-2, no se mezcla.
