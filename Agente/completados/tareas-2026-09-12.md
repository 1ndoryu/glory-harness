# Tareas completadas — 12-09

## 129A-5 - Aprobación tardía congela la tarjeta (12-09)

**Gate PASS.** `sentinel check 129A-5 --stages scripts/quality/stages.json`:
coverage/sccache/sentinel/rust PASS, 0 errores, 0 warnings (5 info
preexistentes). `npx tsc --noEmit` EXIT 0. Commit `7678999`.
Plan archivado en `Agente/planes/completados/`.

**Causa raíz** (verificada en BD + código): turno `44aaf1d0`
(conversación `b4eccce1…`) NUNCA pausó — `done` 1 s después de la petición,
`respuesta_aprobacion {aprobar}` 4 s después del cierre, sin nadie en espera;
la tarjeta quedó en `aprobada · ejecutando…` para siempre.
(1) `main.rs` fijaba `fijar_espera_aprobacion_en_turno(true)` solo si
`autocreada`: con conversaciones existentes el desktop corría en
entre-turnos y 129A-3 eliminó el reenvío, así que nada ejecutaba lo aprobado.
(2) `responder_peticion` devolvía `Ok` aunque no hubiera waiter.

### Qué se hizo
- **F1 `desktop/src-tauri/src/main.rs`:** flag fuera del `if autocreada` —
  el desktop siempre pausa el turno en cada petición; CLI/TUI/daemon/web
  conservan el entre-turnos (flag apagado por defecto).
- **F2 core:** `responder_peticion` → `Result<bool>` (`true` = despertó a un
  turno en espera); `runtime.responder_aprobacion` propaga; tests afirman
  `!desperto` en entre-turnos (p1/p2/p3) y `desperto` en espera (p-w1/p-w2).
- **F3 desktop+front:** `sesion.rs responder_aprobacion` → `Result<bool>` +
  evento `respuesta_aprobacion_sin_espera`; `realTipos.ts`/`transporteTauri.ts`
  `Promise<boolean>`; `aplicarEventos.ts` pinta "el turno ya terminó · envía
  un mensaje nuevo" si no se despertó a nadie; `api.ts` (modo web) devuelve
  `true` (entre-turnos clásico con reenvío del servidor).
- Alivio inmediato: nada colgado en el servidor (turno `ok`, sin
  `errores.log`); basta enviar un mensaje nuevo en esa conversación.

## 129A-2 - Streaming en tiempo real (12-09)

**Gate PASS.** `sentinel check 129A-2 --stages scripts/quality/stages.json`:
coverage/sccache/sentinel/rust PASS, 453 tests ok (3 nuevos de `EmisorVivo`),
0 fallidos. `tsc` EXIT 0 + `vite build` OK (113 módulos, 176.69 kB).
Plan archivado en `Agente/planes/completados/`.

**Causa raíz** (verificada al inicio): el núcleo no emitía nada en vivo —
`on_token` solo acumulaba, el único `Token` salía completo al final y
`reasoning_content` se acumulaba en silencio.

### Qué se hizo
- **F1 core:** evento `RazonamientoDelta { texto }` (`contrato/evento.rs` +
  test de serialización) emitido desde `hojear_stream` (`stream.rs`,
  `SalidasVivo { token, razonamiento }` público para clippy
  `too_many_arguments`); plomero por `red.rs` (las 3 firmas stream).
  `EmisorVivo` en `runtime/tools.rs` (`try_send` throttled 40 ms / 240 chars,
  `Full` → rebuffer, `Closed` → suelta; `enviados` = prefijo ya emitido).
  `llm_llamada(..., en_vivo: bool)` devuelve `(tool_calls, razonamiento,
  enviados)`; el paso principal emite vivo y `gestionar_respuesta_final`
  solo envía la cola `enviados..` (sin duplicados); wrap-up, reintento y
  subagentes van en batch (`false`). `cierre_wrap_up` mudado a `tools.rs`
  (tope 500 líneas de `turno/mod.rs`); `turno` es `pub(crate)` y
  `EstadoTurno.mensajes/respuesta_final` también (lo mínimo).
- **F2 desktop** (`realTipos.ts`, `aplicarEventos.ts`, `turnoReal.ts`,
  `mensajesBloques.ts` `fijarProgreso` + clase `vivo`, `mensajesNucleo.ts`
  `PieTurno.velocidadTokS`, `panelChatTurno.ts` mide tok/s por llamada,
  `panelChatHistorial.ts`, `mensajes.css` latido + `prefers-reduced-motion`):
  bloque "Razonando…" vivo con contador chars/4, summary al cerrar con total
  + segundos, pie `X.X tok/s`.
- **F2 CLI** (`chat.rs`, `tui/bucle.rs`): `RazonamientoDelta` se ignora en el
  stream, `Razonamiento` se resume a 120 chars (stderr / línea de estado).
- **Tests nuevos** (`tools.rs` `pruebas_emisor_vivo`, con channel real):
  envío por umbral, acumulación + envío por intervalo, rebuffer tras canal
  lleno sin perder texto.

### Gotchas
- `TrySendError` exige los tres brazos (`Full` devuelve el valor → rebuffer;
  `Closed` se suelta). `unused_parens` en `if !x.is_empty()` (clippy).
  `AgenteEvento` no tiene `PartialEq` (asserts con `match`).
  Mi propio assert contó mal "hola mundo" (10 chars, no 11) — el fallo era
  del test, no de la lógica.

### Pendiente real
- Ver en `tauri dev` con modelo de razonamiento: bloque vivo intercalado
  pensamiento→texto, contador, tok/s y no-duplicado del `Token` final.

## 119A-2 F4 - Archive/Delete threads por proyecto (12-09)

**Gate PASS.** `sentinel check 119A-2 --stages scripts/quality/stages.json`:
coverage/sccache/sentinel/rust PASS, 459 tests ok, 0 fallidos. `tsc` EXIT 0 +
`vite build` OK (175.20 kB).

### Qué se hizo
- **Persistencia** (`cli/src/persistencia_sqlite/conversaciones/lote.rs`,
  partido de `mod.rs` por limite-lineas 500): `conversaciones_archivar_por_workspace`
  (UPDATE batch reversible hilo a hilo) + `conversaciones_eliminar_por_workspace`
  (SELECT previo de ids + borrados mensajes/acciones/turnos/conversación en `tx`,
  devuelve ids para el vault) + test `conversaciones_batch_por_workspace`.
- **Tauri** (`desktop/src-tauri/src/chat/conversaciones.rs`, registrado en
  `main.rs`): `archivar/eliminar_conversaciones_proyecto` con guard de turno,
  limpieza del vault por hilo y re-anclaje del panel.
- **Web** (`cli/src/comandos/web_datos/areas.rs`, rutas en `comandos/web/mod.rs`):
  `POST .../workspaces/:wid/archivar` → `archivadas`,
  `DELETE .../workspaces/:wid/conversaciones` → `eliminadas` + `actual`.
- **Front**: `convArchivarProyecto/convEliminarProyecto` en `realTipos.ts`,
  `transporteTauri.ts`, `adaptadorReal.ts` (`archivarProyecto/eliminarProyecto`);
  HTTP en `apiConversaciones.ts` nuevo (límite 300 en `api.ts`) + spread en `api.ts`;
  ítems `Archive threads` (un paso, reversible) / `Delete threads` (confirmar en
  dos pasos) en `sidebarMenuProyecto.ts` + handlers con resync y re-anclaje en
  `barraLateralProyecto.ts` (`sidebar.ts`, `barraLateral.ts` cableados).

### Gotchas
- `prepare` + `query_map` con `stmt` de vida corta dentro de `tx` no compila
  (E0597 doble, lib+test). Fix: SELECT previo sobre `conn` (idioma de `mod.rs`),
  borrados en `tx`. Clippy además exigió `is_some_and` en vez de
  `map_or(false, …)` (web `areas.rs` + Tauri `conversaciones.rs`) y retorno
  directo sin `Ok(...)?` en el closure.

### Pendiente real
- Verificación funcional en ventana `tauri dev` real (clic derecho + cada
  acción; pendiente de disco con espacio para el shell debug).

## 119A-5 F3 parcial - Blur de meta solo confirma con cambios (12-09)

**Bug:** escribir un mensaje normal creaba una meta sin usar `/meta`.
Causa: `confirmarEdicion` disparaba `onMetaCambiada` en cada `blur` aunque el
texto no hubiera cambiado; al enviar, la entrada recupera el foco y el `blur`
fijaba (caja con texto) o limpiaba (caja vacía) en silencio.

**Fix** (`desktop/ui/src/componentes/panelMeta.ts`, commit `5fea82e`):
`ultimoConfirmado` como línea base — el `blur` solo confirma si el texto
cambió, y `sincronizarCaja` (sync programático) actualiza la base para no
re-disparar. `tsc` EXIT 0 + `vite build` OK; HMR recargó la ventana del
`tauri dev` en marcha. Resto de F3 (distinguir «sin meta») sigue abierto.

## 129A-1 - Razonamiento visible como summary (12-09)

**Problema:** con `reasoning_effort` el proveedor devuelve `reasoning_content`
pero se perdía (stream y no-stream lo ignoraban); el front solo pintaba
razonamiento en el mock.

**F1 core** (`core/src/nucleo/llm/modelo.rs|stream.rs|red.rs`,
`core/src/contrato/evento.rs`, `core/src/nucleo/runtime/tools.rs`):
`AiStreamResult.razonamiento`, captura de `delta.reasoning_content` y
`/choices/0/message/reasoning_content`, evento único
`AgenteEvento::Razonamiento` al completar (sin razonamiento no se emite).

**F2 cableado + persistencia:** SSE (`agent.event`) y Tauri
(`agente-evento`) reenvían el enum genérico, sin cambios. `llm_llamada`
devuelve `(tool_calls, razonamiento)`; `EstadoTurno.razonamientos` acumula uno
por llamada (bucle + wrap-up; subagente lo descarta); `auditoria.rs`
`persistir_respuesta_final` guarda filas `rol = "reasoning"` ANTES del
`assistant`, `guardar_razonamiento` helper (límite 500 líneas servicio).
`historial_desde_persistencia` filtra `reasoning` (nunca viaja al proveedor;
test nuevo), `listar_mensajes` ordena `creado_en, rowid` (desempata el mismo
segundo), `session ver` etiqueta `[razonamiento]`. Sin migración de schema.

**F3 front** (`realTipos.ts`, `aplicarEventos.ts`, `panelChatHistorial.ts`):
summary cerrado reutilizando `crearRazonamientoCerrado`, en vivo y al
recargar (intercalado por orden de inserción).

**Evidencia:** `tsc` EXIT 0 + `vite build` 175.43 kB; gate
`sentinel check 129A-1` PASS final (0 errores, 0 warnings; 449 tests ok;
INFOs preexistentes en `sidebar.ts` y `context.rs:972` no tocados).
Plan archivado en `Agente/planes/completados/`.

### Pendiente real
- Verificación funcional con modelo que devuelva `reasoning_content` real
  (deepseek/reasoning) en `tauri dev`: ver el summary en vivo, recargar y
  comprobar que persiste intercalado; `/export` no incluye razonamiento.

## 129A-4 — Observabilidad por turno + aprobación fail-loud (12-09)

**Gate PASS.** `sentinel check 129A-4 --stages scripts/quality/stages.json`:
coverage/sccache/sentinel/rust PASS (0 errores, 0 warnings; INFOs
preexistentes `large-interface-isp` en `mensajesNucleo.ts:73`,
`sidebar.ts:73`, `depsCrearPanel.ts:19` no tocados por esta tarea).
`tsc --noEmit` EXIT 0. Plan archivado en `Agente/planes/completados/`.

**Motivo:** sin logs no se podía diagnosticar el atasco
`aprobada · ejecutando…` (`1889f535-11d5-40e5-a3b1-171353a317b0`).

### Qué se hizo
- **F1 persistencia** (`cli/src/persistencia_sqlite.rs`,
  `cli/src/persistencia_sqlite/eventos_turno.rs` nuevo,
  `cli/src/lib.rs`): tabla `eventos_turno`
  (turno_id, tipo, payload_json, creado_en) + columna `peticion_turno` en
  `peticiones_aprobacion` (mapa petición→turno); writer/reader fuera del
  trait core + 2 tests (roundtrip, vínculo). Se omiten `Token`/
  `RazonamientoDelta` por volumen.
- **F2 wiring desktop** (`desktop/src-tauri/src/chat/turno.rs`,
  `sesion.rs`, `main.rs`): `reenviar_eventos` recibe `turno_id` +
  persistencia y anota cada evento (best-effort con `eprintln!`, nunca tumba
  el turno); `responder_aprobacion` registra `respuesta_aprobacion` /
  `respuesta_aprobacion_fallida` (fail-loud: antes fallaba en silencio) +
  `log::anotar` en fallo/sin turno; comando `log_turno(turnoId)`.
- **F3 log de proceso** (`desktop/src-tauri/src/log.rs` nuevo):
  `instalar()` al inicio de `main` (panic hook incluido), solo
  errores/panics, rotación 1 MB (`errores.1.log`), resolución única.
- **F4 visor** (`realTipos.ts` `EventoTurnoLog` + `Transporte.logTurno?`,
  `transporteTauri.ts`, `adaptadorReal.ts` `sesion.logTurno`,
  `orquestador/vistaLogTurno.ts` + `estilos/logTurno.css` nuevos,
  `mensajesNucleo.ts` botón `pie-log` con icono `terminal`,
  `panelChatHistorial.ts` + `panelChat.ts` cableado, `mensajes.css` +
  `tema.css`): botón "ver log" en el pie del turno vivo (la recarga no trae
  `turnoId` y no lo muestra); overlay con filas `<details>` (hora, tipo,
  resumen, JSON íntegro + copiar). Gate encontró 2 defectos reales ya
  corregidos: `log.rs:26` E0271 (`temp_dir` no devuelve `Option`) y
  `vistaLogTurno.ts:37` DOM fuera del boundary (`document.createElement` →
  `el('pre')`, `document.body` → `cuerpo()`).

### Pendiente real
- Verificación funcional en `tauri dev` con binario reconstruido: provocar un
  turno con aprobación, abrir "ver log" del pie y comprobar que lista
  `peticion_aprobacion` → `respuesta_aprobacion`; reproducir el atasco
  `1889f535…` para confirmar qué evento falta.
