# Tareas completadas — 11-09

## 109A-5 — Meta con ciclo de vida (tareas visibles + cierre con evidencia)

**Cerrada con F1–F4 completas.** La tarea no se cierra por fases sino por el
bloque: F1/F2 (10-09) ya estaban HECHO en el roadmap y F3/F4 se completaron hoy.
Plan cerrado en
`Agente/planes/completados/plan-109A-meta-ciclo-vida-2026-09-10.md` (ahí vive el
detalle por fase y los límites honestos de cada una).

### Qué se hizo

- **F1 — Meta como objeto con ciclo de vida:** meta por CONVERSACIÓN (no por
  sesión) con `resolver_meta`, persistencia `meta_texto/iniciada_en/pausada_en/
  logros`, `meta_aplicar` con `&mut` y comandos fijar/limpiar/pausar/reanudar/
  lograr fail-closed.
- **F2 — Tareas visibles atadas a la meta:** `EstadoTodo::EnCurso` (`[/]`),
  evento aditivo `TareasActualizadas`, `emitir_tareas` tras cada acción de `todo`
  y al abrir turno, UI en `tareasMeta.ts`/`tareasMeta.css`. Fix de alcance: el
  plan vivía en el `registry` (por sesión) y se filtraba entre conversaciones;
  ahora `PlanesConversacion` + `cargar_plan_de(conv)` + `olvidar_tareas(conv)`.
- **F3 — Cierre con evidencia:** evento aditivo `MetaLograda { meta, lograda_en,
  elapsed_ms, turno_id }`, `GET/PATCH /api/v1/session/:id/meta` con estado
  completo + logro, `metaAplicar`/`metaLeer` en Tauri, `duracion.ts`, badge en el
  pie del turno (`span.pie-logro`) e historial durable en el panel.
- **F4 — Regla de bloqueo + `/meta` con ciclo de vida:** `BloqueoPlan` en el plan
  (motivo + turnos, acciones `bloquear`/`desbloquear`), conteo por TURNO en el
  runtime, umbral 3 (`UMBRAL_BLOQUEO_TURNOS`) y `ResultadoBloqueo`; al tercer
  turno con el mismo motivo la meta se pausa de verdad (reloj congelado) y se
  publica `MetaPausadaPorBloqueo` antes de `turn.finished`. Un avance real del
  plan levanta el bloqueo. `/meta <texto>` pasa a fijar la meta DURABLE
  (`fijarMetaDurable`) en vez de un borrador sin ciclo de vida.

### Archivos

- `core/src/contrato/evento.rs`, `core/src/herramientas/todo.rs`,
  `core/src/nucleo/runtime/mod.rs`, `core/src/nucleo/runtime/turno/mod.rs`,
  `core/src/nucleo/prompt.rs`.
- `cli/src/servicio/meta.rs`, `cli/src/servicio/sesion.rs`,
  `cli/src/comandos/web/` (`mod.rs`, `meta.rs`, `sse.rs`, `turnos.rs`).
- `desktop/src-tauri/src/workspaces.rs`, `desktop/src-tauri/src/turno.rs`,
  `desktop/src-tauri/src/main.rs`.
- `desktop/ui/src/` (`dominio/tipos.ts`, `tauri/realTipos.ts`,
  `tauri/transporteTauri.ts`, `tauri/adaptadorReal.ts`, `tauri/aplicarEventos.ts`,
  `adaptadores/api.ts`, `adaptadores/apiMeta.ts`, `util/duracion.ts`,
  `componentes/{panelMeta,panelChatTurno,panelChatComandos,metaLogros,tareasMeta}.ts`,
  `orquestador/vistaMeta.ts`, `main.ts`).

### Evidencia

- `cargo test -p glory-harness-core -p glory-harness --lib` → **132 cli + 297
  core pasan**, 0 fallan.
- `cargo clippy -p glory-harness-core -p glory-harness -p glory-harness-desktop
  --all-targets -- -D warnings` → limpio.
- `cargo fmt --check` sin drift en los archivos tocados por F3/F4.
- `npm.cmd --prefix desktop/ui run build` (`tsc --noEmit` + `vite build`) → EXIT 0.
- E2E navegador real (modelo real): pausar congeló el reloj en `en pausa 02:20`,
  reanudar lo reanudó (`02:20→02:33`) y lograr pintó `Meta lograda en 01:01` en el
  pie del turno con `title` = texto de la meta.
- E2E navegador (fixture, sin modelo) sobre el binario: ciclo de vida por HTTP —
  `fijar` → `iniciada_en` + `logros: []`; `pausar` → `pausada_en`; `reanudar` →
  `pausada_en: null`; `limpiar` → `{activa: null, logros: []}`.
- Gate: `sentinel check 109A-5 --stages scripts/quality/stages.json` →
  `coverage` PASS, `sentinel` PASS con **0 errores, 10 warnings, 1 info**
  (baseline). El veredicto global es FAIL **solo** por `sccache-no-configurado`,
  ajeno a esta tarea (registrado como "Gate: etapa Rust y sccache").

### Decisiones y gotchas

- **El conteo es por turno, no por llamada:** N `bloquear` en el mismo turno son
  un solo turno atascado, así que suma el runtime al cerrar, no la tool.
- **Repetir el mismo motivo conserva el contador; un motivo nuevo lo reinicia** y
  cualquier avance del plan lo levanta. Sin esas dos salvaguardas la meta se
  pausaría por un motivo ya resuelto.
- **El evento de pausa sale ANTES de `turn.finished`:** el cliente cierra el
  stream con el fin del turno, así que un aviso posterior se perdería.
- **Un turno fallido no escala** el bloqueo: pausar la meta por un fallo de
  proveedor sería culpar al bloqueo de un problema de red.
- **El prompt instruye el bloqueo, el backend lo cuenta:** declarar `bloquear`
  depende del modelo; el umbral y la pausa son deterministas.
- **`/meta <texto>` era un prefijo ciego con ciclo de vida nulo:** empujaba solo
  el borrador y F1 lee la meta durable, así que la meta del comando no tenía ni
  reloj ni historial. Corregido en F4.
- **El gate empujó dos refactors:** `limite-lineas` obligó a partir
  `comandos/web/` (F3) y `funcion-larga-rs` obligó a extraer `reenviar_eventos` y
  `aplicar_escalada` de `turno_real` (F4). Ambos devuelven el recuento al
  baseline y el segundo además deja la escalada testeable sin métodos privados.

### Prevención / Sentinel

- El baseline del gate es **10 warnings + 1 info**; cualquier recuento mayor en
  los archivos tocados indica que `limite-lineas` o `funcion-larga-rs` está
  disparando y hay que partir el código antes de cerrar, no después.
- El turno `--fixture` debe reenviar la MISMA secuencia de eventos que el real
  (`Done` incluido): si diverge, los E2E sin modelo dejan de servir de oráculo.
- La regla `todo-pendiente` marca cualquier comentario que empiece por
  `TODO|FIXME|HACK|PENDIENTE|XXX`: al reformular un comentario, no dejarlo al
  principio de línea.

### Pendientes reales (no de esta tarea)

- **Badge y plan son estado de ejecución:** no se repintan al reentrar en la
  conversación desde el historial ni sobreviven a reiniciar la app; el
  **historial de logros sí es durable**.
- **Migración con modelo real del bloqueo:** el E2E de la escalada usa el estado
  fijado a mano (3 turnos con el mismo motivo), no un modelo declarando
  `bloquear` tres veces.
- Sigue abierto el resto del bloque 109A: `109A-7` (estilos inline
  en TS), `109A-8` (CSS muerto), `109A-9` (avisos reales del gate) y `109A-10`
  (falsos positivos del medidor), más la tarea independiente "Gate: etapa Rust y
  sccache".

## 109A-6 — Deuda de tamaño: `main.ts`, `panelDerecho.ts` y densidad del desktop

**Cerrada el 11-09.** Tarea independiente sin plan propio (extracción sin cambio
de comportamiento); no tenía fases, así que se cierra con el registro aquí.

### Qué se hizo

- **`desktop/ui/src/main.ts` (304 efectivas → por debajo del techo de 300):** el
  registro y la activación de paneles salen a `orquestador/paneles.ts`
  (`crearGestorPaneles` → `{ panelActivo, activarPanel, avisoGlobal }`).
  `main.ts` sigue declarando `panelesRegistrados` y crea el gestor, pero ya no
  contiene las tres funciones.
- **`desktop/ui/src/orquestador/panelDerecho.ts` (307 → por debajo de 300):** el
  ancho del panel derecho (divisor arrastrable + persistencia) sale a
  `orquestador/panelDerechoAncho.ts` (`crearAnchoPanelDerecho` →
  `{ crearGrip, aplicar, acotar, restaurar }`): constantes `ANCHO_MIN = 260`,
  `FRACCION_MAX = 0.7`, estado `fijado`, clase de arrastre
  `redimensionando-lateral` y guardado en la misma clave `lateral_ancho`.
- **Densidad de `desktop/src-tauri/src/` (10 ficheros planos, techo 10):** los
  módulos se agrupan por dominio en `archivos/` (`archivo`, `filesystem`,
  `vault`), `chat/` (`conversaciones`, `turno`) y `proyecto/` (`git`, `memoria`,
  `workspaces`), cada uno con su `mod.rs`. `main.rs` los reexporta con `use`
  (`use archivos::{archivo, filesystem, vault};` …), así que las rutas ya
  escritas no cambian.

### Archivos

- `desktop/ui/src/main.ts`, `desktop/ui/src/orquestador/panelDerecho.ts`.
- `desktop/ui/src/orquestador/paneles.ts` (nuevo),
  `desktop/ui/src/orquestador/panelDerechoAncho.ts` (nuevo).
- `desktop/src-tauri/src/{archivos,chat,proyecto}/mod.rs` (nuevos) + los 8
  módulos movidos; `desktop/src-tauri/src/main.rs` y `sesion.rs` ajustados.

### Evidencia

- `cargo check -p glory-harness-desktop --all-targets` → OK.
- `cargo clippy -p glory-harness-core -p glory-harness -p glory-harness-desktop
  --all-targets -- -D warnings` → limpio.
- `cargo test -p glory-harness-core -p glory-harness -p glory-harness-desktop`
  → **132 cli + 297 core + 11 desktop**, 0 fallan.
- `npm.cmd --prefix desktop/ui run build` (`tsc --noEmit` + `vite build`) → EXIT
  0, **105 módulos** (antes 103: los dos módulos nuevos entran en el bundle).
- E2E navegador real (`web --puerto 8801 --dir-ui desktop/ui/dist`) con
  Playwright: alternar el panel derecho; arrastrar el divisor (926 px de `#cuerpo`
  → tope `0.7` = **648 px**, con la clase `redimensionando-lateral` activa solo
  durante el gesto); ancho persistido en `lateral_ancho`; **recarga de la página
  → 648 px restaurados y panel reabierto**; sin preferencia arranca en el 50 %
  (463 px); pestaña Files (árbol del workspace), pestaña Git local (lista de
  cambios + diff), menú "Agregar tab"; y selección de sidebar sincronizada al
  activar (`E2E 109A-5 F4` queda `.conv.sel`).
- Gate: `sentinel check 109A-6 --stages scripts/quality/stages.json` →
  `coverage` PASS, `sentinel` PASS con **0 errores, 8 warnings, 1 info**. Los dos
  `limite-lineas` desaparecen del corte (el baseline de 10 warnings del bloque
  109A baja a 8, los 8 `console-production` de 109A-9). El veredicto global sigue
  en FAIL **solo** por `sccache-no-configurado`, ajeno a esta tarea.

### Decisiones y gotchas

- **El gestor de paneles se crea ANTES de `montarVistaBarra`:** la barra recibe
  `avisar` en su montaje. Con una declaración de función bastaba el hoisting; con
  un `const` gestor, referenciarlo antes es TDZ. La sidebar y el panel derecho
  llegan como cierres perezosos (`getSidebar`/`getPanelDerecho`) porque se montan
  después; ningún cierre se invoca durante el montaje.
- **La agrupación por dominio amplió visibilidades de `pub(super)` a
  `pub(crate)`:** antes los módulos colgaban de la raíz y `pub(super)` ya
  alcanzaba todo el crate; tras moverlos a `chat/`, `conv_id_de_panel`,
  `conv_id_de_panel_obligatoria`, `CargaConversacion` y `RestauracionTramo` (y
  `ValorWorkspaces` en `proyecto/`) dejaban de ser visibles desde `main.rs` y
  `sesion.rs`. Se preservó la visibilidad EFECTIVA, no se amplió el contrato.
- **`use super::*` pasa a `use crate::*` en los módulos movidos a subdirectorio,**
  y `use super::{...}` a `use crate::{...}`: `super` deja de ser la raíz del
  crate. `super::` dentro del bloque `mod tests` de cada fichero sigue siendo
  correcto y NO se toca.
- **`ARCHIVO`/`filesystem` usaban `super::archivo::ArchivoLeido` en tres sitios y
  `sesion.rs` usaba `super::conversaciones::…`:** son rutas de módulo, no
  `pub(super)`; `archivo` sigue siendo hermano dentro de `archivos/` (la ruta
  `super::archivo` se mantiene válida), y la de `sesion.rs` pasó a
  `crate::chat::conversaciones::…`.
- **Un ancho persistido llamado 314 px en el E2E no era un defecto del código:**
  fue un artefacto de mi propia secuencia de gestos sintéticos (recargas y
  clics entre medias con el divisor aún "sostenido"). La prueba controlada —partir
  sin preferencia, un solo arrastre, mover el puntero suelto y recargar— da
  siempre el valor correcto, y la lógica de arrastre es idéntica a la anterior.

### Prevención / Sentinel

- El corte canónico del gate baja de **10 a 8 warnings** (0 errores); el
  siguiente techo a vigilar es `console-production` (109A-9) y luego la densidad,
  que ahora está en `src/` 7 entradas, `navegador/` 5, `archivos/` 3,
  `proyecto/` 3, `chat/` 2.
- Antes de mover módulos entre directorios, comprobar visibilidades
  (`pub(super)`/`pub(crate)`) y rutas `super::`/`crate::`: el compilador las
  señala todas, pero conviene resolverlas en el mismo bloque.
- El "Matiz" del corte compartido (`902c45e` marca `cli/src/servicio/sesion.rs`
  en 506 efectivas; el build que fija este repo, `1587c59`, no) sigue siendo
  cierto, pero ya no hay ningún `limite-lineas` en el corte canónico.

## 109A-7 — Sacar los estilos inline del TS (`cssInlineScript`, 52 errores reales)

**Cerrada el 11-09.** Plan: `Agente/planes/completados/plan-109A-7-estilos-inline-2026-09-11.md`
(4 fases, todas cerradas en el mismo bloque).

### Qué se hizo

- **52 de 52 `cssInlineScript` eliminados de `desktop/ui/src` sin un solo
  `sentinel-disable`.** Criterio aplicado (el mismo del plan): **estado discreto →
  clase CSS**; **valor medido en tiempo de ejecución → variable CSS publicada con
  `setProperty('--x')`** y consumida por `var(--x, fallback)`. La regla de VarSense
  (`<varsense>/src/core/analyzeDocument.ts:38`) exime expresamente
  `.style.setProperty('--…')`, así que las medidas legítimas caben en el sistema de
  diseño sin excepción justificada.
- **F1 — menús y selector de modelo** (`28` hallazgos): `menu.ts` publica
  `--menu-ctx-top/left` y arranca oculto con `visibility:hidden` + `.visible`;
  `entradaContexto.ts` publica `--ctx-detalle-top/left` y usa `.visible`;
  `menuComandos.ts` publica `--menu-cmd-top/left`; `selectorModelo.ts` cambia los
  estilos del submenú por las clases `abierto`/`abajo`/`voltear`.
- **F2 — navegador, nota de proyecto y anotaciones** (`14`): `panelNavegador.ts`,
  `panelNavegadorControles.ts` y `modalProyecto.ts` pasan a `hidden`;
  `anotaciones.ts` usa `canvas.hidden` y la relación de aspecto como variable.
- **F3 — alturas medidas** (`13`): `entradaBarras.ts` publica `--entrada-alto`
  (tope 5 líneas), `panelMeta.ts` publica `--pm-alto` (tope 3 líneas) y
  `panelChat.ts` publica `--entrada-reserva` desde su `ResizeObserver`.
- **F4 — resto** (`4`): `plataforma/webview.ts` inyecta una hoja
  `<style id="gh-resaltar-estilo">` y conmuta la clase `gh-resaltar` (mismo patrón
  que el `scriptSeleccionActivar` ya existente); `util/portapapeles.ts` da la clase
  `copia-temporal` a su textarea fuera de pantalla.

### Archivos

- TS: `componentes/{menu,entradaContexto,menuComandos,selectorModelo,panelNavegador,
  panelNavegadorControles,modalProyecto,anotaciones,entradaBarras,panelMeta,panelChat}.ts`,
  `plataforma/webview.ts`, `util/portapapeles.ts`.
- CSS: `entrada.css`, `menuComandos.css`, `modalProyecto.css`, `navegador.css`,
  `panelMeta.css`, `layout.css`.

### Evidencia

- `npm.cmd --prefix desktop/ui run type-check` → EXIT 0.
  `npm.cmd --prefix desktop/ui run build` → EXIT 0, `index-CP6bvONh.js` 165.05 kB,
  `index-BYSy243t.css` 51.05 kB.
- VarSense 2.2.1 `scan` sobre el árbol: **`cssInlineScript` 0** (venía de 52) y
  `severityCounts` `{error:0, warning:0, information:0, hint:0}`. `orphan-classes`:
  ninguno de los nombres nuevos (`visible`, `abierto`, `abajo`, `copia-temporal`,
  `nav-anotaciones`, `gh-resaltar`, `anotaciones-aspecto`) aparece como huérfano.
- E2E navegador real (`web --puerto 8801 --dir-ui desktop/ui/dist`, Playwright),
  midiendo **computed styles**, no solo que compile:
  - **Menú de modelo:** `.menu-ctx visible` con `visibility: visible`, `top: 868.6px`,
    `left: 34.8px`, 89×230 px, 12 entradas; el submenú queda `.menu-sub abierto abajo`
    con `display: block`, `top: -115.2px`, `bottom: 0px`, alto 142 px; con `voltear`
    → `left: -249px`, `right: 228.4px`; Escape lo cierra.
  - **Menú de comandos (`/`):** `hidden: false`, `top: 694.6px`, `left: 34.8px`,
    225×524 px, 8 entradas y la primera `/ayuda`; al vaciar el textarea desaparece.
  - **Crecimiento del textarea:** vacío 16 px → 2 líneas 32 px → 7 líneas 80.11 px
    (tope de 5) con `scrollHeight 112` y `overflow-y: auto`; al vaciar vuelve a 16 px.
  - **Detalle de contexto:** `.ctx-detalle visible`, `top: 903.6px`, `left: 394.6px`,
    50×135 px, 1 fila; un clic en el documento lo cierra.
  - **Panel meta:** con foco queda `panel-meta inactivo expandido`; 5 líneas →
    48.06 px (tope de 3) con `scrollHeight 80`; 2 líneas → 32 px; al perder el foco
    se revierte la clase y `--pm-alto` desaparece; forzando `.oculto` →
    `display: none` y `margin-bottom: 0px`.
  - **Pestaña Navegador:** `hidden: false` y `display: flex` a 976×442 px;
    `.nav-captura` con `hidden` → `display: none`; **al volver a Files la pestaña
    queda `hidden: true` → `display: none`** (es la prueba de que la regla
    `[hidden]` gana al `display:flex` de la clase, que era el riesgo del cambio).
  - **Portapapeles (ruta de reserva):** forzando `navigator.clipboard.writeText` a
    rechazar, «Copiar ID» pasa por el textarea temporal: capturado en su `select()`
    con `class="copia-temporal"`, `position: fixed`, `opacity: 0`, conectado al DOM
    y el texto correcto; `execCommand('copy')` una vez; y ya fuera del DOM al acabar.
- **Límite declarado (no verificado en runtime):** `anotaciones.ts` (módulo huérfano,
  sin consumidor) y `codigoResaltar` (`plataforma/webview.ts`, solo alcanzable por
  CDP con Tauri) quedan **tree-shaken**: `gh-resaltar` y `resaltar` no aparecen en
  el bundle construido. Del segundo sí se comprobó el script generado contra un DOM
  mínimo: sintaxis OK, sin `.style.`, añade la clase `gh-resaltar`, inyecta la hoja
  `.gh-resaltar{outline:2px solid red !important;outline-offset:-1px !important;}` y
  retira la clase al vencer el timeout.
- Artefacto construido cotejado: en el JS están `copia-temporal`, `menu-ctx-top`,
  `pm-alto` y `entrada-alto`; en el CSS, las 16 reglas nuevas (`.menu-ctx.visible`,
  `.ctx-detalle.visible`, `--menu-ctx-top`, `--ctx-detalle-top`, `--menu-cmd-top`,
  `.menu-sub.abierto`, `.menu-sub.abajo`, `--entrada-alto`, `--pm-alto`,
  `--entrada-reserva`, `.copia-temporal`, `.nav-anotaciones`, `.nav-captura[hidden]`,
  `.panel-navegador[hidden]`, `.proyecto-modal-label[hidden]`, `--anotaciones-aspecto`).
- Gate: `sentinel check 109A-7 --stages scripts/quality/stages.json` → `coverage`
  PASS · `sccache` FAIL (`sccache-no-configurado`, ajeno) · `sentinel` PASS con
  **0 errores, 8 warnings, 1 info**. Sin hallazgos nuevos: los 8 warnings siguen
  siendo los `console-production` de 109A-9. FAIL global solo por `sccache`.

### Decisiones y gotchas

- **La regla de VarSense exime `setProperty('--x')`:** por eso el remedio de las
  medidas en tiempo de ejecución fue publicarlas como variables CSS y no pedir una
  excepción `sentinel-disable`. Queda como patrón del proyecto.
- **`[hidden]` pierde contra `display:flex`:** hubo que declarar
  `.panel-navegador[hidden]`, `.nav-captura[hidden]` y `.proyecto-modal-label[hidden]`
  con `display:none`; sin ellas el atributo no ocultaba nada.
- **`overflow-y` al fijar la altura:** el código original lo ponía condicionalmente;
  ahora la regla declara `overflow-y: auto` permanente, y la barra solo aparece
  cuando el contenido supera el tope — mismo resultado visual, sin estado en TS.
- **`.nav-anotaciones` no tenía ninguna regla CSS** (era 100 % inline): se creó con
  `aspect-ratio: var(--anotaciones-aspecto, 4 / 3)`.
- **`g.style.position = 'relative'` era redundante:** el CSS ya lo declaraba; se
  retiró en vez de convertirlo en clase nueva.
- Medir antes de pintar era el riesgo real de las alturas: el posicionamiento se
  resolvió dejando la visibilidad en el CSS (oculto por defecto, `.visible` al final
  de posicionar) y el tope de líneas se calculó con el `line-height` computado.

### Prevención / Sentinel

- El corte canónico del gate **no cambia con esta tarea** (0 errores, 8 warnings,
  1 info): los estilos inline eran deuda de **VarSense**, y el gate de este repo no
  ejecuta esa etapa, así que el avance solo se demuestra con el binario directo.
- Conviene verificar el **artefacto construido** y no solo el `src`: dos rutas de
  esta tarea quedaron tree-shaken y no llegan al bundle.
- Antes de convertir un estilo inline en clase, cotejar el orden de la cascada
  (`.proyecto-modal-nota` lo exigió) y si la regla base usa `display:flex`, porque
  entonces el atributo `hidden` necesita su propia regla.

## 109A-8 — Retirar el CSS muerto confirmado (`claseHuerfana`)

**Cerrada el 11-09.** Tarea independiente sin plan propio (borrado de CSS con
cotejo nombre a nombre antes de tocar nada).

### Qué se hizo

- **Medición de partida:** `orphan-classes` de VarSense 2.2.1 sobre el workspace
  (**4516 archivos**). El roadmap traía un techo de ≤22 candidatos de un corte
  anterior; el cotejo nombre a nombre lo reduce a **9 nombres sin ningún uso fuera
  de las hojas**.
- **7 eran CSS muerto real y se retiraron:**
  `files-visor-aviso` (`files.css`), `lateral-grip` (`layout.css`: la regla base y
  su ocultado en la media query de `#paneles`), `navegador-grip` y
  `redimensionando-navegador` (`navegador.css`) y los tres patrones `trama-45` /
  `trama-135` / `trama-puntos` (`variables.css` más sus anulaciones del tema oscuro
  en `tema.css`).
- **2 son falsos positivos de la regla y NO se tocaron:** `git-diff-adicion` y
  `git-diff-eliminacion`, porque `gitDiff.ts:149` compone la clase con plantilla —
  `` `git-diff-linea git-diff-${tipo}` `` con `tipo ∈ {adicion, eliminacion,
  contexto}`. Quedan registrados en 109A-10.
- **Los otros 14 nombres** del techo del roadmap (`ctx-pista`, `ctx-lleno`, `barra`,
  `archivada`, `conv-proyecto`, `memorias-chip`, `memorias-nota`, `global`,
  `git-adiciones`, `git-eliminaciones`, `redimensionando-sidebar`,
  `redimensionando-lateral`, `rotulo-cambios`, `ic-xs`…) **tienen uso textual real
  en el TS** y no se tocaron.
- **Comentarios obsoletos corregidos en el mismo bloque** porque describían los
  elementos retirados: la cabecera de `navegador.css` decía que el ancho lo
  «arrastra el `.navegador-grip`» y `panelNavegador.ts` que el panel es
  «redimensionable por grip». Verificado que **hoy no hay grip para el navegador**:
  no lo crea el TS y **nadie fija `--navegador-ancho` desde JS** (solo se consume
  con respaldo de 480 px). El único grip derecho es `panel-derecho-grip`.

### Archivos

- `desktop/ui/src/estilos/files.css`, `layout.css`, `navegador.css`,
  `variables.css`, `tema.css`.
- `desktop/ui/src/componentes/panelNavegador.ts` (solo el comentario).

### Evidencia

- **Cotejo:** cada nombre reportado se buscó en el árbol del UI (`.ts`/`.css`/
  `.html`) y, para los 9 finalistas, también en todo el repo con `git grep`, más una
  búsqueda de composiciones dinámicas (`${…}`, `'…' +`) por si el nombre se armaba en
  tiempo de ejecución. Tras retirar los 7, la lista de candidatos queda en **2** (los
  falsos positivos) y `git grep` de los 7 nombres retirados en `desktop/*` no
  devuelve nada.
- `npm.cmd --prefix desktop/ui run build` → EXIT 0. CSS **51.05 kB → 49.89 kB**
  (`index-B2Nr3XRL.css`); el JS no cambia de tamaño (165.05 kB): solo salieron reglas.
- CSS construido: `trama-`, `navegador-grip`, `lateral-grip`, `files-visor-aviso` y
  `redimensionando-navegador` a **0**; intactos los vecinos que se quedan
  (`.files-visor-codigo`, `.nav-url-barra`, `body.redimensionando-lateral`,
  `body.redimensionando-sidebar`, `--panel-derecho-ancho`, `.ic{`, `.ic-xs`,
  `.ic-spin` y `html[data-tema=oscuro] .cursor`).
- **Smoke en navegador** (web `:8801`, con el UI reconstruido):
  - Arrastre del divisor derecho (`.panel-derecho-grip`, 9×976 px):
    `--panel-derecho-ancho` **443 → 563 px**, con `body.className =
    "redimensionando-lateral"` **solo durante el gesto** y
    `localStorage.lateral_ancho = "563"` al soltar. Es justo la regla de `layout.css`
    que se conserva al lado de la borrada.
  - Panel Files abriendo `README.md`: `.files-visor-codigo` con **199**
    `.files-visor-linea` de 14.04 px y cabecera «README.md — 199 líneas»; ningún
    `.files-visor-aviso` (confirma que la regla retirada no tenía elemento).
  - Pestaña Navegador: `.panel-navegador` visible a 562 px, `.nav-url-barra` 33 px y
    **sin** `.navegador-grip` en el DOM.
  - Tema oscuro: `data-tema='oscuro'` aplica (fondo `#111`, texto `#eee`) y hay **0**
    elementos con clase `trama-*`.
- Gate: `sentinel check 109A-8 --stages scripts/quality/stages.json` → `coverage`
  PASS · `sentinel` PASS **0 errores, 8 warnings, 1 info** (idéntico al corte previo)
  · `sccache` FAIL por la tarea independiente del gate. 10 archivos en el scope.

### Decisiones y gotchas

- **La regla `claseHuerfana` no resuelve nombres compuestos.** Ni por plantilla
  (`` `git-diff-${tipo}` ``) ni por ternario (`'ic' + (pequeno ? ' ic-xs' : '')`). El
  cotejo tiene que buscar el **prefijo** y leer el punto donde se compone; mirar solo
  la cadena completa produce borrados erróneos.
- **`orphan-classes` barre el workspace entero** (4516 archivos, con
  `data/referencias-cli/**` dentro): su total global (2617 marcas hoy) **no es
  comparable** con el corte de la consola del 10-09 (258 marcas sobre 224 archivos).
  El número útil es el recuento por carpeta propia (`desktop/ui/src/estilos`).
- **`--navegador-ancho` se consume y nunca se escribe desde JS:** el panel del
  navegador no es redimensionable hoy. Se documenta en el código; cambiarlo queda
  fuera de alcance.
- **`--lateral-ancho` sigue viva** (la consume `.chat[data-panel='lateral']` con
  `flex: 0 0 var(--lateral-ancho)`): lo retirado era solo su grip, ya inexistente. El
  ancho del panel derecho vive en `--panel-derecho-ancho`
  (`orquestador/panelDerechoAncho.ts`), no en `--lateral-ancho`.
- Las 7 clases retiradas **no podían renderizar nada** (nadie las aplicaba), así que
  la verificación visual es un no-op por construcción; aun así se hizo el smoke de los
  paneles cuyas hojas se tocaron, porque `variables.css` y `tema.css` son globales.

### Prevención / Sentinel

- El corte canónico **no cambia con esta tarea** (0 errores, 8 warnings, 1 info):
  `claseHuerfana` es de VarSense y el gate de este repo no ejecuta esa etapa.
- Quedan **302 marcas** de `claseHuerfana` en `desktop/ui/src/estilos` que son deuda
  de la regla, no del proyecto (109A-10). Regla práctica: **no borrar CSS por
  parecido**; de los 9 candidatos, 2 eran falsos positivos.
- Un nombre de clase sin uso textual **sí puede estar vivo** si se compone en tiempo
  de ejecución; y un elemento con clase inexistente deja reglas CSS zombis
  (`navegador-grip`, `lateral-grip`) que conviene retirar con su comentario.

## 109A-9 — El panel del navegador deja de fallar en silencio (`console-production` ×8)

**Cerrada el 11-09.** Tarea independiente sin plan propio (los 8 hallazgos estaban ya
localizados por el corte del gate).

### Qué se hizo

- **Los 8 `console.*` del gate están todos en el panel del navegador** y se sustituyen
  por el canal de aviso que ya usa el orquestador (`deps.avisar` → `avisoGlobal` →
  `panelActivo().avisoLocal`, es decir, un aviso visible en el chat activo):
  - `panelNavegadorSeleccion.ts` (4): activar la selección (77), detenerla (116) y las
    dos precondiciones del modo — requiere la app de escritorio (127) y navegador
    cerrado (131).
  - `panelNavegador.ts` (3): capturar (101), atrás (140) y adelante (156).
  - `orquestador/navegadorVista.ts` (1): no se pudo abrir el navegador (97).
- **Contrato nuevo:** `PanelNavegadorOpciones.onAviso` **obligatorio** y
  `SeleccionNavDeps.avisar`; `navegadorVista.ts` los conecta a `deps.avisar`.
  `montarPanelNavegador` pierde el `= {}` por defecto (incompatible con una opción
  obligatoria) y tiene **un único consumidor**, así que el cambio no arrastra a nadie.
- **Un fallo silencioso más, encontrado al verificar:** el botón «Capturar pantalla»
  está **habilitado en modo web** y su handler hacía `if (!esTauri) return;` → clic sin
  efecto y sin mensaje. Ahora avisa igual que su hermano «Seleccionar elemento»
  (que ya lo llevaba en el `title`).

### Archivos

- `desktop/ui/src/componentes/panelNavegador.ts`,
  `desktop/ui/src/componentes/panelNavegadorSeleccion.ts`,
  `desktop/ui/src/componentes/panelNavegadorTipos.ts`,
  `desktop/ui/src/orquestador/navegadorVista.ts`.

### Evidencia

- `npm.cmd --prefix desktop/ui run build` → EXIT 0: JS **165.10 → 165.17 kB**
  (`index-Cw56nsxD.js`, solo lo que pesa el aviso nuevo), CSS **49.89 kB** sin cambios.
- `git grep -F console` sobre `desktop/ui/src/**` → 3 coincidencias, **todas en
  comentarios** que explican la decisión (`panelNavegador.ts:22`,
  `panelNavegadorTipos.ts:33`, `navegadorVista.ts:46`); ninguna llamada.
- Gate: `node …/sentinel/out/cli/index.js check 109A-9 --workspace .
  --stages scripts/quality/stages.json` → `coverage` PASS (0/0/0) · `sentinel` PASS
  **0 errores, 0 warnings, 1 info** → **los 8 warnings bajan a 0** · `sccache` FAIL por
  la tarea independiente del gate. 7 archivos en el scope.
- **Smoke en navegador** (web `:8801`, con la consola de la página instrumentada para
  acumular `console.warn/error`): de los 8 sitios, **3 son alcanzables en modo web** y
  los tres quedan verificados **con la consola vacía**:
  - «Seleccionar elemento» → aviso visible «la selección de elementos requiere la app
    de escritorio (WebView2)» (antes `console.warn`).
  - «Atrás» → «no se pudo navegar atrás» (antes `console.error`).
  - «Adelante» → «no se pudo navegar adelante».
  - «Capturar pantalla» → «la captura de pantalla requiere la app de escritorio
    (WebView2)».
  - Sin regresión: «Recargar» sigue recargando (iframe en `https://example.com`) y el
    panel monta a 562×976 px con su pestaña activa.
- **Limitación declarada:** los 5 sitios restantes (captura/atrás/adelante por IPC,
  activar y detener la selección, y el «navegador cerrado») **exigen Tauri/WebView2** y
  no son alcanzables en modo web; no se fuerzan ni se fingen. Se comprueba que compilan
  y que pasan por el **mismo** `avisar`, que es una única ruta de código para los 8.

### Decisiones y gotchas

- **El canal ya existía:** `avisar(texto, meta, detalle)`. No hizo falta
  `sentinel-disable`, ni UI de estado nueva, ni cambiar el diseño del panel.
- **`onAviso` obligatorio, no opcional:** si fuera opcional, un consumidor futuro
  podría volver a dejar el fallo sin salida; con el tipo obligatorio omitirlo **no
  compila**. Como solo lo consume `navegadorVista.ts`, el coste del cambio es nulo.
- Los textos se normalizan a **minúscula inicial**, como el resto de avisos del
  proyecto (`'no se pudo copiar: …'`).
- El `todo-pendiente` de `core/src/nucleo/context.rs:972` sigue apareciendo como **1
  info**: es el falso positivo de la regla (`///` de doc comentario + «todo» en prosa),
  **no** deuda de este código y **fuera** de esta tarea (109A-10).
- Los avisos del panel salen en el **chat activo**, no dentro del panel: es el mismo
  comportamiento que ya tenía el orquestador para el navegador, así que no se
  introduce un patrón nuevo.

### Prevención / Sentinel

- El corte del gate canónico pasa a **0 errores, 0 warnings, 1 info**: lo único que
  mantiene el veredicto global en FAIL es `sccache-no-configurado` (tarea independiente
  «Gate: etapa Rust y sccache» del roadmap).
- **Hallazgo registrado como 109A-11:** en modo web «Atrás/Adelante» nunca navegaron
  con páginas de otro origen (`SecurityError` al leer `contentWindow.history` ajeno).
  Antes fallaban en silencio, ahora lo dicen; el arreglo real (pila propia de URLs) es
  una tarea aparte.
- Regla práctica: **un botón visible sin efecto es un fallo silencioso aunque no haya
  `console.*`**; al añadir o revisar una rama web conviene mirar los
  `if (!esTauri) return;` del panel.

