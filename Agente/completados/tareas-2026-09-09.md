# Tareas completadas — 2026-09-09

## 089A-18 — Coherencia de layout, persistencia y workspace (HECHO 09-09)

- Qué se hizo:
  - Ocultar el panel derecho oculta también su barra de tabs sin desmontar el toggle ni la botonera; mostrar conserva tabs y contenido.
  - Se serializan y restauran visibilidad, ancho, tabs y tab activa; se validan ids, se descartan `chat:nuevo-*` y se mantiene compatibilidad con estados legacy sin `visible`.
  - La restauración Tauri/web es asíncrona y espera la carga de chats laterales antes de activar la tab guardada.
  - Los estados hover/active/focus afectados usan tokens semánticos compartidos, también en tema oscuro.
  - El selector de workspace usa el panel originador; un lateral cambia a borrador y no crea conversación hasta el primer envío.
- Archivos:
  - `desktop/ui/src/componentes/{panelDerecho,panelChatTipos}.ts`
  - `desktop/ui/src/orquestador/{arranque,crearPanel,laterales,panelDerecho,persistencia}.ts`
  - `desktop/ui/src/main.ts`
  - `desktop/ui/src/estilos/{variables,tema,tabs,barraSuperior,layout,entrada,files,git,navegador,launcher}.css`
  - `Agente/planes/completados/plan-089A-18-coherencia-layout-workspace-2026-09-09.md`
  - `roadmap.md`
- Evidencia:
  - `npm run type-check` PASS y `npm run build` PASS en `desktop/ui` (85 módulos Vite).
  - `git diff --check` PASS.
  - `npm run quality:doctor`: `issues: []`, `readyForGate: true`, política válida con hash; herramienta fijada Sentinel 0.7.8 (el `sentinelVersion` 0.7.4 de doctor es el runtime global) y VarSense 2.2.1.
  - Navegador: ocultar/mostrar panel, launcher, Files, Navegador tras reload, tab activa, tema oscuro y tokens verificados.
  - Navegador: chat lateral cambiado a `glory-harness` quedó en borrador; principal intacto; 8 conversaciones antes del cambio y 9 después del primer envío; respuesta recibida correctamente.
- Gate:
  - `sentinel check 089A-18 --workspace . --stages scripts/quality/stages.json` ejecutado realmente.
  - Resultado **FAIL**: coverage PASS (0/0/0), Sentinel FAIL (317 errores, 205 warnings, 39 hints), con hallazgos en deuda base de `data/referencias-cli/**` y Rust preexistente. Matiz: el reporte del check declara `invalid-policy`/`mode: observe`/`blocked: false` (identidad de política no disponible para el ejecutable), así que el FAIL es informativo y la etapa fail-closed (coverage) pasó. No se declara PASS ni se mezcla esa deuda con esta tarea.
- Limitaciones:
  - El iframe de `https://example.com` informó `net::ERR_ABORTED` al recargar, aunque la tab se restauró.
  - No se validó la webview nativa Tauri en esta sesión.
  - Sentinel/VarSense no ejecutan E2E visual ni inspeccionan toda la representación CSS; la aceptación visual queda respaldada por el navegador.
- Prevención: conservar la separación entre gate estático y validación visual; tratar el FAIL base como tarea independiente y no desactivar reglas para ocultarlo. No se hizo push ni deploy.

## 089A-17 — Git local estilo Synara: staged, changes y diff seleccionado (HECHO 09-09)

- Qué se hizo:
  - El panel Git dejó de mostrar un diff global en un `<pre>` y ahora separa
    `Staged` y `Changes`, con contador y estadísticas de adiciones/eliminaciones.
  - El diff permanece oculto hasta seleccionar un archivo y muestra una sola
    comparación en una caja con ruta, hunks, números de línea y líneas de contexto.
  - Tauri y HTTP devuelven `diff_staged` (`git diff --cached`) y
    `diff_unstaged` (`git diff`), conservando `diff` como compatibilidad.
  - Los archivos no rastreados (`??`) generan diff de archivo nuevo mediante
    argumentos estructurados; el presupuesto combinado queda limitado a 1 MiB y
    a 256 archivos. La selección distingue grupo y ruta para no resaltar dos
    veces el mismo archivo staged/unstaged.
  - Actualizado el contrato documentado del endpoint web.
- Archivos:
  - `desktop/ui/src/componentes/panelGit.ts`
  - `desktop/ui/src/componentes/gitDiff.ts`
  - `desktop/ui/src/estilos/git.css`
  - `desktop/src-tauri/src/git.rs`
  - `cli/src/comandos/web_datos/git.rs`
  - `Agente/documentacion/modo-web-2026-09-07.md`
  - `roadmap.md`
- Evidencia:
  - `cargo fmt --all -- --check` PASS.
  - `cargo check --workspace` PASS.
  - `cargo test --workspace` PASS: 263 cli + 8 desktop, sin fallos.
  - `desktop/ui`: `npm run build` PASS (`tsc --noEmit` + Vite).
  - `git diff --check` PASS.
  - `npm run quality:doctor`: `readyForGate: true`, `issues: []`.
  - `sentinel check 089A-2 --workspace . --stages scripts/quality/stages.json` PASS:
    coverage y sentinel, 0 errores/0 warnings/0 info.
- Gotchas:
  - `git diff` no incluye `??`; se genera el patch de archivo nuevo con
    `git diff --no-index /dev/null <ruta>` desde el workspace, sin shell concatenado.
  - La validación visual en navegador/Tauri no fue posible en esta sesión; queda
    pendiente confirmar representación responsive y foco/teclado en ventana real.
- Prevención/Sentinel: el gate incremental cubre los seis archivos del cambio y
  mantiene el límite de salida en backend; no se hizo push ni deploy.

## 089A-16 — Saneamiento total del gate (HECHO 09-09)

- Qué se hizo:
  - F1 errores TS + splits grandes (`main.ts`, `sidebar.ts`, `api.ts`,
    `mensajes.ts`, `entrada.ts`, `panelChat.ts`, `panelNavegador.ts`,
    `tauri/real.ts` 862→165 fachada).
  - F2-resto (commit `edbc8d9`): boundary DOM/window (`util/dom.ts` +
    `plataforma/ventana.ts` dueño único de `window.*` +
    `plataforma/webview.ts` snippets CDP), 14 call sites reescritos,
    barrels puros (`entradaMontaje.ts`, `mensajesNucleo.ts`,
    `adaptadorReal.ts`), `directoryExceptions` + `node_modules` en
    `sentinel.config.json`. Gate `089A-16-F2RESTO` PASS 0E/0W/10I.
  - B-ISP (commit `f292869`): 10 interfaces grandes partidas en
    subinterfaces cohesivas por `extends` (nuevo
    `orquestador/barraLateralTipos.ts`; `tsc` EXIT 0). Gates
    `089A-16-BISP` PASS 0E/0W/1I y `089A-16-BISP2` PASS 0E/0W/0I.
  - F3 (commit `caec783`): cero warnings Rust — `sort_by_key`,
    closures redundantes, imports muertos (`sesion_memoria_borrador`
    eliminado de `web.rs`), `Default` derivable, `or_else`→`or`,
    docs `rustdoc` (listas, tabs, líneas vacías, continuaciones con
    backslash), IIFE WebView2 extraídas a helpers con valores owned
    (gotcha: `&ICoreWebView2` no vive lo bastante en
    `Box::new(move ...)` + `async` anidado; firmado con owned
    resuelve). `cargo fmt` limpio, `cargo clippy --workspace
    --all-targets` 0 warnings, `cargo test --workspace` 363 OK
    (91+1+263+8).
  - Cierre: `tsc` EXIT 0, `vite build` OK (85 módulos), gate full
    `sentinel check 089A-16` **PASS 0/0/0** (coverage + sentinel).
- Archivos: `desktop/ui/src/{util/dom,plataforma/ventana,plataforma/webview,
  componentes/entradaMontaje,componentes/mensajesNucleo,tauri/adaptadorReal,
  orquestador/barraLateralTipos}.ts`, 10 `*Tipos.ts` ISP, 22 `.rs`
  (cli/core/desktop), `sentinel.config.json`, `roadmap.md`, plan archivado
  en `Agente/planes/completados/plan-089A-16-saneamiento-total-gate-2026-09-09.md`.
- Evidencia: `.quality-reports/check/089A-16/latest.md` (PASS 0E/0W/0I);
  `.quality-reports/check/089A-16-{F2RESTO,BISP,BISP2}/latest.md`.
- Gotchas:
  - `tgrep`/`rg`/`head`/`wc` ausentes en pwsh: `Select-String`,
    `Select-Object`, `(Get-Content).Count`.
  - `npx tsc` instala `tsc@2.0.4` erróneo; usar
    `./desktop/ui/node_modules/.bin/tsc.cmd --noEmit -p desktop/ui`.
  - `edit` con CRLF: el `oldString` debe coincidir con `\r\n`.
  - `CoTaskMemPWSTR::from` pide `&str`: con `String` owned usar
    `.as_str()` dentro del closure `move`.
  - `cargo fmt` normaliza ficheros no tocados (imports, newline final):
    incluirlos en el commit F3 es correcto.
- Prevención/Sentinel: gate full verde con `coverage` fail-closed (089A-15);
  portable rules y `directoryExceptions` ya en `sentinel.config.json`.
- Pendientes reales: re-gate `unwrap-produccion-rs` (~9 prod de 41
  `.unwrap()`); verificación visual UI 089A-3/4 + historia 089A-5.
