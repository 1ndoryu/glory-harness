# 2026-09-13 — fix visual 129A-8 (botón ver-en-Cambios + fuente resumen)

## 129A-8/129A-12 fix 2 — botones Aceptar/Rechazar + fuente UI de Synara (commit 6f99c4b)
- El usuario señaló los botones del snippet `.cambios-acciones`: `.cambios-boton` solo
  fijaba tamaño/padding y caía al estilo gris por defecto del navegador. Fix:
  `panelCambios.ts` añade clase `btn` (Aceptar, Rechazar, Ver/Ocultar diff) y
  `cambios.css` usa `.btn.cambios-boton` (especificidad doble, no depende del orden
  de inyección ya que `cambios.css` se importa desde el componente).
- Fuente del tema Synara: `tema-synara.css` añade `--font-ui` (pila del sistema =
  Segoe UI en Windows, la de Synara según `synara/apps/web/src/index.css:389-411`,
  sin CDN), body a `--font-ui` con peso 400 (el 300 global es para Departure Mono),
  raíz 14px solo en synara, y re-fija a `--font` (Departure Mono local) las
  superficies de código: `.git-diff`, `.git-diff-cabecera .git-diff-ruta`,
  `.visor-plano`, `.herramienta .resultado`, `.aprobacion .args`.
- Evidencia: `tsc` EXIT 0; `vite build` OK 1.85s; `C:\tmp\gh-129a8fix2-smoke.mjs` 6/6
  (borde/fondo canónico en claro+synara); `C:\tmp\gh-129a8fix4-smoke.mjs` TODO OK
  (peso 400, Segoe UI en UI, mono en código); capturas `-fix2-` y `-fix4-synara.png`
  revisadas. `cambios.css` no está en el cascade de `index.css`, verificado.
- Nota: `.cambios-boton-rechazar` sigue sin estilo propio (no existe regla): Rechazar
  se ve igual que Aceptar. Si se quiere distinción de peligro, es tarea aparte.

## 129A-8 fix — paridad visual del resumen de turno (commit 44a8577)
- Reporte del usuario: el botón "ver en Cambios" no se veía como los demás botones
  y la fuente del resumen no cuadraba. Causa: `.resumen-turno-ver` era un botón-texto
  sin borde (`background:transparent; border:none`, solo `underline` en hover) y el
  bloque usaba `font-size:var(--xs)` (~9.4px) frente a `var(--sm)` (~11px) del mensaje.
- Fix (2 archivos, +8/-10):
  - `desktop/ui/src/componentes/resumenTurno.ts`: botón pasa a `btn resumen-turno-ver`
    (reutiliza el `.btn` canónico con overrides en los 3 temas, incluido Synara).
  - `desktop/ui/src/estilos/mensajes.css`: `.resumen-turno` `font-size --xs → --sm`
    (igual que `.msg-asis`); `.resumen-turno-ver` conserva solo posición en la fila
    (`flex:none; margin-left:auto; white-space:nowrap`), la pintura la pone `.btn`.
- Evidencia: `npm run type-check` EXIT 0; `npm run build` OK 3.10s;
  `C:\tmp\gh-129a8fix-smoke.mjs` 3/3 (fuente 11.05px = mensaje, borde 1px, fondo opaco)
  + capturas `C:\tmp\gh-129a8fix-claro.png` y `C:\tmp\gh-129a8fix-synara.png` revisadas:
  "ver en Cambios" idéntico al botón de referencia en ambos temas.
- Limitación registrada: gate `sentinel check` NO re-ejecutado para este fix —
  preflight de disco exige 8 GB libres y hay 7.86 GB (`C:\tmp\glory-target/glory-harness`
  11 GB, rama actual `main`, no borrable). La tarea `GloryTmpSweep` lo recupera cada hora.
  Verificación proporcional aplicada (tsc + build + CDP vivo en 2 temas); el Rust no se
  tocó y el CSS quedó probado en navegador real.

## 129A-8/129A-12 fix 2 — botones Aceptar/Rechazar + fuente UI de Synara (commit 6f99c4b)
- El usuario señaló los botones del snippet `.cambios-acciones`: `.cambios-boton` solo
  fijaba tamaño/padding y caía al estilo gris por defecto del navegador. Fix:
  `panelCambios.ts` añade clase `btn` (Aceptar, Rechazar, Ver/Ocultar diff) y
  `cambios.css` usa `.btn.cambios-boton` (especificidad doble, no depende del orden
  de inyección ya que `cambios.css` se importa desde el componente).
- Fuente del tema Synara: `tema-synara.css` añade `--font-ui` (pila del sistema =
  Segoe UI en Windows, la de Synara según `synara/apps/web/src/index.css:389-411`,
  sin CDN), body a `--font-ui` con peso 400 (el 300 global es para Departure Mono),
  raíz 14px solo en synara, y re-fija a `--font` (Departure Mono local) las
  superficies de código: `.git-diff`, `.git-diff-cabecera .git-diff-ruta`,
  `.visor-plano`, `.herramienta .resultado`, `.aprobacion .args`.
- Evidencia: `tsc` EXIT 0; `vite build` OK 1.85s; `C:\tmp\gh-129a8fix2-smoke.mjs` 6/6
  (borde/fondo canónico en claro+synara); `C:\tmp\gh-129a8fix4-smoke.mjs` TODO OK
  (peso 400, Segoe UI en UI, mono en código); capturas `-fix2-` y `-fix4-synara.png`
  revisadas. `cambios.css` no está en el cascade de `index.css`, verificado.
- Nota: `.cambios-boton-rechazar` sigue sin estilo propio (no existe regla): Rechazar
  se ve igual que Aceptar. Si se quiere distinción de peligro, es tarea aparte.
  (Nota 13-09 tarde: con 139A-1 `cambios-acciones`/`cambios-diff` ya no existen;
  Aceptar/Rechazar viven en la cabecera del `git-diff-caja`.)

## 139A-1 — Cambios: con git solo git; sin git estilo git, `git-diff-caja` con radio, `.btn` 5px 16px 7px
- Revisión de lo planificado: el plan 129A-7 decía "mitad superior vault + mitad
  inferior git siempre visibles" (`tareas-2026-09-12.md:246`) — esa decisión era el
  error, no un malentendido: con git la lista separada es redundante.
- Cambio (139A-1): `panelCambios.cargar()` consulta `git.estado()` una sola vez; si
  `aplicable` muestra SOLO el panel git (vía `panelGit.fijar()`, sin refetch);
  si no, pinta el vault con las MISMAS clases git (sección `Cambios`, filas
  `git-entrada` código `M`, `git-diff-caja` vía `separarEntradas` + diffs vivos
  envueltos en cabecera `diff --git`), una fila por ruta (el turno más reciente),
  Aceptar/Rechazar en la cabecera del diff (`pintarDiff` acepta `acciones`),
  revisado por ruta (atenúa la fila). `panelGit` gana `seleccionar(ruta)` para
  que `revelar()` (129A-8) funcione en ambos modos. Eliminadas
  `cambios-lista/grupo/entrada/ruta/turno/acciones/diff/separador/vacio` de
  `cambios.css` (quedan contenedor, `.btn.cambios-boton`, `.cambios-estado`,
  `.git-entrada.revisado`) y sus radios synara. API (`recargar`,
  `registrarCambioVivo`, `revelar`) y transportes sin cambios.
- Tres pedidos del usuario en el mismo bloque: `git-diff-caja` con radio 10px en
  synara (+ `overflow:hidden` para recortar fondos de filas); `.btn` a
  `padding:5px 16px 7px` (mi +1px arriba había empeorado: la tinta se asienta
  baja) con revert de `.aviso-accion` y `.cambios-boton` a sus valores.
- Evidencia: `tsc` EXIT 0; VarSense scan13 0/0 (131 archivos);
  `C:\tmp\gh-139a1-smoke.mjs` 14/14 (git-solo, vault-estilo-git, dedupe, stat
  +1−1, aceptar→revisado, revelar en ambos modos, vacío); fixture
  `gh-synara-radio2.mjs` 18/18 (`gitDiffCaja=10px`, btn 5px/16px); capturas
  `gh-btn-zoom.png` (botón centrado) y `gh-synara-radio.png` revisadas.

## Wrap en diff y files + push (commit a272eba, push `d595fc1..a272eba`)
- Reporte del usuario: `git-diff-caja` se expandía más allá del panel derecho y el
  texto de los archivos no tenía saltos de línea. Causa: `git.css` fijaba
  `.git-diff-caja{min-width:max-content}` + `.git-diff-linea{min-width:max-content;
  white-space:pre; ...minmax(max-content,1fr)}`; `files.css` igual con
  `.files-visor-linea{min-width:max-content}` + `.files-visor-texto{white-space:pre}`.
- Fix (`git.css`, `files.css`, +10/-9): caja a `max-width:100%`; línea a
  `minmax(0,1fr)`, `min-width:0`, `white-space:pre-wrap`;
  `.git-diff-texto` y `.files-visor-texto/plano` con `overflow-wrap:anywhere`
  (texto `flex:1;min-width:0` para encoger en el flex). Sin cambios de API ni TS.
- Evidencia: `C:\tmp\gh-wrap-smoke.mjs` 5/5 en marco de 360px (caja 338,
  scroll-X 0 en ambos, línea 300 caracteres envuelta a 130px/97px de alto) +
  `C:\tmp\gh-wrap.png` revisada; VarSense scan14 0/0 (131 archivos,
  `C:\tmp\varsense-gh-scan14.json`). Push autorizado por el usuario, `main` en sync.

## Fondo opaco en cajas fijadas (commit ddf82e3, sin push)
- Pedido: todas las cajas que se fijan con fondo, no transparentes. Inventario:
  `fixed` con fondo ya OK (`.menu-cmd`, `.menu-ctx`, `.ctx-detalle`,
  `.ajustes-pagina`, `.modal-fondo/.modal`, `.toast-item`); sin fondo solo el
  slot `.aprobaciones-fijas` y la tarjeta `.aprobacion` (solo borde).
- Fix: `background:var(--fondo)` en ambos (`mensajes.css:.aprobacion`,
  `entrada.css:.aprobaciones-fijas`). En flujo no cambia nada (el chat ya es fondo).
- Evidencia: `C:\tmp\gh-fondo-smoke.mjs` 2/2 (slot y tarjeta `rgb(14,14,14)`
  opaco en synara) + `C:\tmp\gh-fondo.png`; VarSense scan15 0/0 (131 archivos).

## 139A-2 Cambios multi-repo estilo VSCode (sin commit/push, decisión usuario)
- F1 backend (`proyecto/git.rs`, comandos en `main.rs`): `RepoGit{nombre,ruta,
  prefijo}` (`prefijo` = ruta repo relativa al área, `""` = el área misma),
  `workspace_git_repos` (BFS descendente, profundidad default 2, tope 20,
  ignorados node_modules/target/dist/build/tmp/Temp/__pycache__/.venv/dotfiles,
  anidados colapsados al padre) + `workspace_git_estado_en` con contención;
  `EstadoGit.rama` (`branch --show-current`, detached `HEAD@<7>`, sin commits
  `None`). Tests con asserts de prefijo; fix de carrera: temporal único por
  test (dos tests compartían `gh-repos-{pid}` en hilos paralelos).
- F2 transporte+frente: `GitTransport.estado(ruta?)+repos()`, `montarPanelGit
  ({ruta?})`, cableado `realTipos/transporteTauri/api/adaptadorReal/
  panelDerecho`; modo web con errores explícitos (sin soporte multi-repo).
  `panelCambios`: sección por repo (`nombre · rama`, contador, diff vivo vía
  `panelPara`), huérfanos del vault de ESTA conversación en `Sin repositorio`,
  sin duplicar lo cubierto por git legible; repo ilegible → oculto y sus
  cambios caen a huérfanos. Lógica pura en `util/reposUtil.ts`.
- F3 refresco: `alTerminarTurno` (vía `notificarTurnoFin`) +
  `alCambiarConversacion` (`onConversacionCambio`, cierres perezosos por
  `crearPanel/depsCrearPanel/main`); botón manual `Actualizar`
  (`.cambios-barra`); abrir panel revalida (`abrirCambiosEn → revelar → recargar`);
  aceptar/rechazar revalidan (`recargar()` tras el resultado).
- F4: split por `limite-lineas` (host 259 + `vaultCambios.ts` 255, extraído
  pintar/selección/aceptar/rechazar/revisados sin cambiar comportamiento).
  Sin captura CDP (limitación: sin backend Tauri en navegador no hay repos;
  evidencia alternativa smoke node). Evidencia: `tsc` EXIT 0;
  `C:\tmp\gh-repos-smoke.cjs` 14/14 (atribución por prefijo, backslash,
  partición huérfanos); analyze 0 errores/0 warnings/7 info (ISP
  preexistente); `sentinel check 139A-2` **PASS** (coverage/sccache/sentinel/
  rust 35.3s, 474 tests ok). Plan archivado en
  `Agente/planes/completados/plan-139A-2-cambios-multirepo-2026-09-13.md`;
  roadmap sin 139A-2. Gotcha: `arbol_repos()` compartido = flaky en paralelo.

## 139A-3 Fix multi-repo + temas (sin commit)
- Bug reportado: `no se pudo consultar Git en workspace-manager` (`missing
  required key ruta`). Causa: `transporteTauri.workspaceGitEstado` enviaba
  `{ raiz: ruta }` y `workspace_git_estado_en` (`git.rs:103`) espera `ruta:
  String`. Fix en una línea (`{ ruta }`); `workspace_git_repos` sin args OK
  (`profundidad: Option` → default 2).
- Toast: era superficie invertida (pegote claro en oscuro/synara) → superficie
  normal (`--texto`/`--fondo`) con `var(--borde)` (`toast.css`) + radio-caja
  en synara. Menú: `#000` literal → `var(--borde)` en `.menu-ctx`, `.menu-sep`
  y `.menu-sub` (`entrada.css`); en claro queda idéntico (`--borde:#000000`),
  en oscuro/synara el borde vuelve visible.
- Evidencia: `tsc` EXIT 0; fixture `C:\tmp\gh-toast-menu-fixture.html` 21/21
  (7 checks × claro/oscuro/synara: fondo/texto/borde toast, borde menú/sep/
  sub, radio) + `C:\tmp\gh-toast-menu-synara.png` revisada; `sentinel check
  139A-3` **PASS** (coverage/sccache/sentinel/rust). Roadmap sin 139A-3.

## 139A-4 Input principal sin scrollbar fantasma (sin commit)
- Reporte: el `textarea#principal-input` mostraba scroll con una sola línea
  (`--entrada-alto: 17px`). Causa: con `overflow-y: auto` fijo, cualquier
  discrepancia subpíxel entre el alto publicado y el contenido enseña el
  scrollbar (el +1px solo atenuaba el caso de una fila).
- Fix: helper testeable `util/autoAlto.ts` (`ajustarTextArea`: mide con
  `scrollHeight`, topa a 5 líneas, publica `--entrada-alto` y conmuta
  `overflowY` a `hidden` mientras crece / `auto` solo al llegar al tope);
  `entradaBarras.ajustarEntrada` delega (misma firma y llamadas);
  `entrada.css` pasa el defecto a `hidden` y actualiza el comentario.
- Evidencia: `tsc` EXIT 0; fixture `C:\tmp\gh-auto-alto-fixture.html` 14/14
  (L1–L5 `hidden` + contenido cabe + el alto crece; L6 `auto` + tope de 5 +
  scroll disponible; bundle esbuild del helper real); `sentinel check 139A-4`
  **PASS**. Roadmap sin 139A-4.

## 139A-5 Repos en caja con cabecera unificada y lista minimizable (sin commit)
- Pedido: el span suelto de rama sobra (unificar en la cabecera), cada repo
  tiene que ir en caja (margen/padding) y su lista de cambios plegable,
  minimizada por defecto.
- Cambio (`panelCambios.ts`, `cambios.css`): la cabecera del repo es un
  `<button>` único con marcador ▸/▾ + título `nombre · rama` (sin rama →
  solo nombre) + contador; eliminado el span `.git-repo-rama` y su CSS.
  La caja lleva `border: 1px solid var(--borde)`, margen y radio
  (`var(--radio-caja, 6px)`), y se cancela el `border-top` de adyacencia de
  `git.css` para hijos directos de `.panel-cambios-repos`. `colapsado` nace
  `true` por repo y sobrevive a las recargas (los wraps se reutilizan);
  el revelado (`revelarAhora`) expande el repo objetivo.
- Evidencia: `tsc` EXIT 0; fixture `C:\tmp\gh-repo-caja-fixture.html` con el
  bundle esbuild del componente real **15/15** en Chrome headless (2 cajas,
  minimizadas por defecto, sin `.git-repo-rama`, `repoA · main` / `repoB`,
  cabecera botón con `aria-expanded`, `display:none` plegado, toggle
  expandir/minimizar, revelado expande); `sentinel check 139A-5` **PASS**
  (30 archivos, rust 17.8s, 0 errores). Roadmap sin 139A-5.

## 139A-6 Sección colapsable plana compartida + líneas únicas (sin commit)
- Pedido: quitar la caja 139A-5 (borde incoherente), misma sección para
  repos y "Sin repositorio" (ambos minimizados, el revelado expande); más
  el reporte de borde doble (el `border-top` sin acotar sumaba 2px).
- Cambio: nuevo `util/seccionColapsable.ts` (`crearSeccionColapsable`:
  section + button cabecera con marcador, título, contador,
  `aria-expanded`, `fijarColapso`); `panelCambios` y `vaultCambios` lo usan
  (cuerpo `.seccion-cuerpo`, plegado con regla compartida; el vault conserva
  el plegado en clausura y coordina diff + `.lista-plegada`).
- Líneas únicas (1px por frontera): `git.css` re-acota el divisor a
  `.git-lista > .git-seccion + .git-seccion` (solo Staged/Changes); la
  cabecera-botón conserva su línea en un solo sitio (`cambios.css`, antes
  duplicada); el vault plegado anula el borde de su lista vacía.
- Bug latente cazado por el fixture: `vault.pintar` consumía el revelado
  pendiente vía `tomarRevelado` y se comía el de un repo cuando había
  huérfanos; `cargarRepos` lo consume una vez y lo pasa explícito
  (`pintar(..., revelado?)`). `vaultCambios.ts` en 296 líneas (límite 300).
- Evidencia: `tsc` EXIT 0; fixture `C:\tmp\gh-repo-caja-fixture.html`
  **24/24** en Chrome headless (plano sin cajas, sin border-top externo,
  línea única de cabecera, Staged|Changes intacto, misma cabecera/marcador
  repos+vault, minimizados al nacer, lista plegada sin borde y con borde al
  expandir, `repoA · main`, toggle del vault, revelado en repo y huérfano
  con diff) + captura `C:\tmp\gh-plano-139A-6.png`;
  `sentinel check 139A-6` **PASS** (32 archivos, rust 27.6s, 0 errores;
  7 infos preexistentes fuera del alcance). Roadmap sin 139A-6.

## 139A-7 Cambios eficiente: sin re-análisis al cambiar de conversación (sin commit)
- Causa: `recargar()` monolítico (repos → N estados → listar → repintado
  total) disparado por fin de turno, cambio de conv, abrir tab, cambio de
  área, vivas, rechazos y revelados, sin coalescar ni comparar; A→B→A
  re-escaneaba todo aunque git no depende de la conv.
- F1 front: bloque multi-repo extraído a `componentes/cambiosRepos.ts`
  (`consultarGit` usa `git.resumen?.()` con fallback al fan-out; el mock del
  fixture delega en crudos SIN contador para comparar antes/después);
  `util/vueloUnico.ts` (single-flight+trailing, total arrastra a vault);
  `panelCambios.recargarVault()` (solo vault, cero git;
  `main.ts:alCambiarConversacion` → `recargarVault()`); pintar-solo-si-cambió
  (firma global git + firma vault; revelado se consume solo en full).
- F2 backend: `workspace_git_resumen` (`git.rs`: descubrir+estado por repo en
  1 IPC, secuencial v1 sin caché; repo ilegible viaja con `est:None`+`error`;
  `ResumenRepo{repo,est,error}` espeja el front sin mapear; `descubrir_repos`
  delega en `rutas_repos`+`describir_repo` sin cambiar orden) + registro en
  `main.rs`; front `transporteTauri.workspaceGitResumen`,
  `Transporte.workspaceGitResumen?` (web lo omite → fan-out),
  `adaptadorReal.gitResumen` condicional, `panelDerecho` lo pasa solo si existe.
- Evidencia: `tsc` EXIT 0; fixture `C:\tmp\gh-eficiencia-fixture.html` antes
  `repos=6 estado=8 listar=4 resumen=0` 6/6 → después
  `repos=0 estado=0 listar=5 resumen=3 disparos total=4 vault=2` 6/6 (el +1 es
  el trailing del burst); 24/24 139A-6 sin regresión;
  `sentinel check 139A-7` **PASS** (37 archivos, rust 97s, 0 errores;
  3 infos ISP preexistentes fuera del alcance). Roadmap con 139A-7 marcada;
  plan archivado en `Agente/planes/completados/`.

## 139A-8 F2 SQLite bajo async + historial acotado (R1/R2/R3, gate PASS)
- Qué: `con_conn` (`spawn_blocking` + `Arc` + `Mutex`, `busy_timeout` 5 s) en
  `cli/src/persistencia_sqlite.rs`; `puerto.rs` (17 métodos
  `AgentPersistence`) y `tareas.rs` reescritos a `con_conn` con args
  clonados; `conversaciones/mensajes.rs` nuevo (partido de `mod.rs` por
  limite-lineas): `leer_mensajes` (`creado_en >= ?` + `LIMIT`, orden
  `creado_en, rowid`), `listar_mensajes_desde`, `leer_conversacion` (SELECT
  por PK); `sesion.rs` resuelve el punto ANTES de leer y usa
  `listar_mensajes_desde`; auto-nombre por `conversacion_obtener` puntual.
- Medición (`cli/tests/p95_turno.rs`, 5 k msgs, 50 turnos, N=2): baseline
  p95=87,81 ms → tras fix p50=1,90 ms p95=3,83 ms (≤0,5×).
- Evidencia: DoD `mensajes_desde_filtra_en_sql_y_obtener_es_puntual`;
  `cargo test -p glory-harness` 157 lib + 2 bin + p95 verdes;
  `sentinel check 139A-8` **PASS** (477 tests, 0 warnings; el warning
  limite-lineas de `conversaciones/mod.rs` se resolvió con la partición).
- Gotchas: `GLORY_MIN_FREE_GB` no atraviesa `sentinel check` (fija 8 GB);
  gate desbloqueado borrando `debug/incremental` (4,66 GB, se regenera).
  `C:\tmp` ~11,7 GB (target activo) sobre el tope 7 GB: higiene pendiente.
  Nota: con este commit los 4 tests `cli/infra` que bloqueaban el gate de
  139A-9 vuelven a verde (eran WIP de esta fase).
- Pendiente real: F3 filesystem/git/vault (K2,K8,K7,K10,S8).
