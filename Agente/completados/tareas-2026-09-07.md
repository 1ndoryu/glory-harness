## 069A-1 F6: Pipeline multimodal (completado 2026-09-07)
### Que se hizo
1. **AgenteEvento::ToolNavegador** - nueva variante en `evento.rs` con accion, ok, url, selector, captura_base64 y descripcion. Serializa tagged JSON correctamente.
2. **AgentToolResult::evento_extra** - nuevo campo opcional en `tool.rs` con constructor `ok_con_evento()`. Permite que cualquier tool emita un evento adicional junto a ToolResult.
3. **ToolNavegador capturar** - emite ToolNavegador con captura_base64 como evento_extra desde `navegador.rs`.
4. **Runtime relay** - `ejecutar_tool_aprobada` extrae evento_extra y lo envia al canal tras ToolResult.
5. **Frontend event handler** - `main.ts` y `real.ts` actualizados para el nuevo schema tool_navegador con captura_base64.
6. **Panel navegador** - `panelNavegador.ts` expone `actualizarCaptura()` en la API publica.
7. **web.rs SSE** - arreglado para axum 0.8.9 (Sse::new requiere TryStream<Ok=Event>); usa Event::default().data(msg) + futures_util::StreamExt::map.
8. **schedule_cmd.rs** - anadido navegador: None faltante (pre-existente).

### Archivos modificados (solo F6 + fixes necesarios)
- core/src/contrato/evento.rs
- core/src/herramientas/tool.rs
- core/src/herramientas/navegador.rs
- core/src/herramientas/comando.rs
- core/src/contrato/pregunta.rs
- core/src/nucleo/runtime/turno/permisos.rs
- core/src/nucleo/runtime/subagente.rs
- core/src/nucleo/runtime/tools.rs
- core/src/nucleo/subagente.rs
- cli/src/schedule_cmd.rs
- cli/src/comandos/web.rs
- desktop/ui/src/tauri/real.ts
- desktop/ui/src/main.ts
- desktop/ui/src/componentes/panelNavegador.ts

### Compilacion
- glory-harness-core: 0 warnings, compila limpio
- glory-harness (lib): compila (5 warnings pre-existentes no relacionados)
- glory-harness-desktop: fallos pre-existentes en navegador.rs (Tauri 2 IPC, no relacionados con F6)

### Gate
- sentinel check FAIL (solo issues pre-existentes en web.rs: axum-ruta-sintaxis, broadcast-mutex)
- Issues pre-existentes, no causados por F6

### Pendiente real
- desktop compilation bloqueada por navegador.rs (Tauri 2 macro issues, fuera del alcance de 069A-1)

## 069A-2 F2-F6 modo web (completado 2026-09-07)

### Qué se hizo
- F2 (`e731c74`): turnos SSE con cookie `gh_sesion`, `event:` + snapshot
  `ready`, `turn.finished` obligatorio, fixture real, paridad socket.
- F3 (`40e47d0`): conversaciones/config/providers/workspace, `SesionComun`
  con `cambiar_workspace`/`reconstruir`, SQLite WAL compartida (77 tests).
- F4 (`8b597e7`): `Transporte` + `transporteTauri()` en `real.ts`,
  `crearTransporteApi`/`crearAdaptadorApi` en `adaptadores/api.ts` (fetch +
  EventSource con cookie, degradación explícita rewind/vault/meta),
  factoría en `main.ts` (`?api=`/`gh_api`/mismo origen).
- F6-backend (`c28e653`): body 256 KiB (413), 16 sesiones (429), TTL 24h
  (410 + aborto huérfano), `mensaje_largo`→413, auditoría logs sin secretos,
  matriz daemon-TCP vs web-HTTP, doc `Agente/documentacion/modo-web-2026-09-07.md`.
- F5a evaluación read-only (baseline `c28e653`): F5b NO justificada (sin
  ganancia funcional, colisión con refactor ajeno de `main.rs`).

### Archivos
- cli/src/comandos/web.rs, web_turnos.rs, web_datos.rs, cli/Cargo.toml
- desktop/ui/src/tauri/real.ts, adaptadores/api.ts, main.ts, componentes/panelChat.ts

### Evidencia
- `cargo test -p glory-harness --lib`: 79/79 verde (21 web incl. TTL+429).
- `tsc --noEmit` UI: ficheros propios limpios (4 errores ajenos concurrentes).
- `vite build`: bundle ok. E2E curl contra `web --fixture`: session+cookie,
  config, turns, SSE en vivo `ready`/`turn.started`/`agent.event`/`turn.finished`.
- Render en navegador real sin verificar (sin Playwright): limitación registrada.

### Gate
- `sentinel check 069A-2` FAIL con 13 errores + 6 warnings + 1 info.
  Veredicto inspección: OK CON OBSERVACIONES, sin defecto real propio:
  11× `axum-ruta-sintaxis` son falso positivo (regla exige `:id` de matchit
  0.7.3; el lock fija matchit 0.8.4 + axum 0.8.9 donde `{id}` es correcto, y
  el E2E en vivo devuelve 200 en rutas parametrizadas — aplicar la sugerencia
  rompería el router); 2× `broadcast-mutex` son observación de diseño
  (fan-out SSE recomendado por tokio; 16 sesiones loopback sin contención
  posible); warnings/info ajenos (herramientas/, desktop en refactor).
  Acción: ninguna (no reparar código correcto ni tocar alcance ajeno).
  Reporte: `.quality-reports/check/069A-2/latest.md`.

### Adenda 07-09: fix del falso positivo en Sentinel
- Regla `axum-ruta-sintaxis-rs` hecha version-aware en glory-sentinel
  (commits `08aaf25` + `6baf87c`, pusheados a origin/main): resuelve
  matchit/axum desde el Cargo.lock subiendo del fichero; `{param}` correcto
  en matchit 0.8+ (flaggea `:param`), legacy ante stack desconocido.
  Módulo nuevo `src/analyzers/rustAxumStack.ts` (budget ADR 0001).
  Tests: 6/6 regla + equivalencia + broadcast + reglas-nuevas verdes;
  `quality:setup` (compile + check:core + smoke:lsp + mocha completa) PASS.
- Adoptado en glory-harness: staging en `6baf87c`, `quality-tools.json` +
  `sentinel.lock.json` repineados, `doctor` verde (readyForGate, 0 issues).
- La meta web se completó posteriormente con `PATCH /api/v1/session/:id/meta`,
  normalización, límite de 8.000 caracteres, autorización de sesión y uso en
  el siguiente turno en modo `meta`.
- Gate 069A-2 re-ejecutado: 13→2 errores (los 11 FP eliminados). Quedan 2
  `broadcast-mutex` (diseño SSE consciente: 16 sesiones loopback, sin
  contención posible; refactor a mpsc-por-suscriptor desproporcionado) +
  warnings/info ajenos. Sin defecto real propio.
## 069A-9: Seleccionar elemento de la página + el modelo navega (07-09)
### Que se hizo
1. **Seleccionar elemento (frontend, solo UI):** boton "Seleccionar elemento"
   (icono `seleccionar`/mouse-pointer) en el panel navegador. Inyecta JS en la
   child webview via `navegador_js`: modo activo con outline 2px + crosshair;
   al hacer hover resalta el elemento; al hacer clic captura `{pagina: URL,
   selector CSS robusto (#id o tag+clases+nth-child), etiqueta corta, texto
   visible}` en `window.__ghSel__`. Polling lee el resultado y lo adjunta como
   badge (`adjunto-badge`) al chat activo. El siguiente mensaje antepone
   `[elemento de la pagina <url> — selector CSS: <sel> — etiqueta: <tag>]`.
   Modo web (iframe): solo aviso "requiere la app de escritorio (WebView2)".
   Archivos: panelNavegador.ts, entrada.ts, panelChat.ts, tipos.ts, iconos.ts,
   entrada.css, navegador.css, main.ts.
2. **Fix causa raiz (Rust):** el modelo NO veia `navegador_reflejo` al cambiar
   de modelo (p.ej. a deepseek-v4-flash-free). Causa: al cambiar modelo el
   front llama `reconfigurarSesion` (Tauri) → `SesionComun::reconfigurar()` →
   `reconstruir(opciones)` con `navegador: None` → el runtime nuevo no registra
   la tool (fail-closed). `SesionComun` ahora conserva el puerto
   (`pub navegador`) y `reconfigurar`/`cambiar_workspace` lo reinyectan.
   Archivo: cli/src/servicio/sesion.rs.

### Evidencia E2E seleccion (driver CDP, 9/9)
- Panel navegador abierto, child navega al fixture
  http://127.0.0.1:8797/fixture-sel.html via UI.
- Modo seleccion activo (`__ghSelActivo__` true); hover resalta `#btn-uno`
  (clase gh-sel-resaltado + crosshair).
- Click captura `{selector:"#btn-uno", etiqueta:"button#btn-uno.accion",
  texto:"Botón uno", pagina:"http://127.0.0.1:8797/fixture-sel.html"}`.
- Badge visible `elemento: button#btn-uno.accion · "Botón uno"`; boton vuelve
  a normal; mensaje enviado con el badge.
- Modo web: click "Seleccionar" loguea "requiere la app de escritorio".

### Evidencia E2E navegacion del modelo (con fix)
- Compilado: `C:\tmp\glory-target\glory-harness\debug\glory-harness-desktop.exe`,
  SHA256 8D42A38393060BB3098CFB4B710E334CDE9D797503ACB189C113BB1D2A862E8D.
- Modelo real (deepseek-v4-flash-free) tras el fix: LLAMO `navegador_reflejo`
  (visible en el chat como bloques "navegador_reflejo ... ok").
- Child webview NAVEGO a http://127.0.0.1:8797/fixture-sel.html y
  `window.__contadorClicks` = 1 (hizo clic en `#btn-uno`).
- Comparacion directa: la conversacion SIN fix respondio "no tengo herramienta
  de navegador interno" (navegador_reflejo ausente de su catalogo); la CON fix
  la uso. Confirmacion directa del fix.
- El turno fallo SOLO al final al generar la respuesta de sintesis: caida
  momentanea de TODOS los providers (deepseek key invalida ****shlo, commandcode
  503, glory en cooldown). NO es problema del fix: la tool ejecuto ok.

### Compilacion / gate
- cargo build desktop: OK (solo warnings ajenos pre-existentes: unused Arc en
  navegador.rs, leer_max_ventana dead code en main.rs).
- Gate: no ejecutado completo (bloqueado por deuda ajena pre-existente en
  main.rs/web.rs en refactor; ver notas del arbol).

### Archivos (esta sesion)
- cli/src/servicio/sesion.rs (fix: conservar puerto navegador)
- desktop/ui/src/componentes/panelNavegador.ts, entrada.ts, panelChat.ts,
  main.ts, tipos.ts, iconos.ts
- desktop/ui/src/estilos/entrada.css, navegador.css
- roadmap.md

## 069A-10: Persistencia web de modelo, modo y razonamiento (07-09)

### Qué se hizo
El selector de modelo, modo y nivel de razonamiento no perduraba al recargar la página en el modo web (`http://127.0.0.1:8760`). Causa raíz:

1. **`web_datos.rs::guardar_config`** (F1) solo reconfiguraba el runtime en memoria; no persistía `provider`, `modelo`, `modo`, `razonamiento` en SQLite antes de la recarga.
2. **`sesion.rs::resolver_opciones`** (F2) no leía `proveedor`, `modelo`, `modo` de la BD; solo restauraba `workspace` y `nivelRazonamiento` pero el resto quedaba siempre `None`.
3. **`api.ts`** (F3) enviaba `proveedor` (clave en español del front), pero el backend `ParcheConfig` espera `provider`.
4. **`main.ts::opcionesArranque`** (raíz). Enviaba los valores actuales del front al abrir sesión, sobrescribiendo la configuración persistida en la BD. Ahora envía campos vacíos y el backend resuelve desde SQLite.

### Archivos modificados
- `cli/src/comandos/web_datos.rs` (+41/-5): persistir 4 claves antes de reconfigurar
- `cli/src/servicio/sesion.rs` (+21/-14): `resolver_opciones` restaura provider/modelo/modo; `reconfigurar` conserva valores actuales
- `desktop/ui/src/adaptadores/api.ts` (+3/-0): mapear `proveedor` → `provider`
- `desktop/ui/src/main.ts` (+4/-6): `opcionesArranque` con campos vacíos

### Compilación
- `cargo build --bin glory-harness`: OK en `C:\tmp\glory-target\glory-harness`
- `npm run build` (desktop/ui): OK
- `get_errors` en los 4 archivos: 0 errores

### Evidencia E2E (API + navegador)
1. **PATCH** `{provider:"deepseek", modelo:"deepseek-v4-flash-free", modo:"meta", razonamiento:"high"}` → 200, config devuelve los cuatro valores.
2. **Nueva sesión POST** → `modelo:"deepseek/deepseek-v4-flash-free"` (resuelto desde BD).
3. **GET config** en la sesión nueva → `provider:"deepseek", modelo:"deepseek-v4-flash-free", modo:"meta", razonamiento:"high"`.
4. **Recarga de página** → UI muestra `deepseek-v4-flash-free`, `Alto`, `Meta` en los controles.
5. **Estado `main`** ahead 1, 4 archivos modificados + `red.rs` (069A-9 preexistente).

### Gate
- `sentinel check 10A-1` → **FAIL** preexistente: `tool-release-unpublished` (el commit `6baf87c2` de Sentinel 0.7.8 no es alcanzable desde refs de release). No es defecto propio.
- Sin defecto real propio. Cambio verificado funcionalmente con E2E API + navegador.

### Pendiente
- Gate bloqueado por deuda de infraestructura (pin de Sentinel). No se puede ejecutar el gate canónico hasta que el dueño del core de Sentinel publique una ref de release alcanzable.
- Push no autorizado.

## 069A-11 — Inicio del panel meta y acceso web local sin token (07-09)

### Qué se hizo
- `desktop/ui/src/main.ts`: el panel meta se inserta después de `.selector-workspace-box` y se oculta mientras el panel es un borrador (`conversaId === null`); se muestra tras el primer envío o al cargar una conversación existente.
- `cli/src/comandos/web.rs`: `GLORY_HARNESS_WEB_TOKEN` pasa a ser opcional. Sin la variable, `POST /api/v1/session` funciona sin `Authorization` y el servidor escucha solo en `127.0.0.1`; con token explícito se conserva la autenticación maestra y el bind en todas las interfaces.
- `desktop/ui/src/adaptadores/api.ts`: el adaptador no exige `?token` en modo local y solo envía `Authorization` cuando existe.
- Se preservó el cambio ajeno preexistente en `desktop/ui/src/tauri/real.ts` (eliminación de `telemetria`).

### Evidencia
- `npm run build` en `desktop/ui`: OK.
- `cargo build --manifest-path cli/Cargo.toml --bin glory-harness` con `CARGO_TARGET_DIR=C:\tmp\glory-target\glory-harness`: OK.
- `GET /healthz`: `{"ok":true}`.
- `POST /api/v1/session` sin token: HTTP 200 y cookie `gh_sesion` HttpOnly.
- Navegador en `http://localhost:8760/`: en “Nueva conversación”, `.panel-meta` quedó `display:none`; `.selector-workspace-box` quedó antes de `.caja` sin solapamiento. Tras enviar `mensaje de prueba`, el fixture creó la conversación y `.panel-meta` pasó a `display:block` debajo del selector (el selector se oculta al existir conversación), con `.caja` después del panel.
- `get_errors` en `main.ts`, `api.ts` y `web.rs`: sin errores.
- `C:\tmp` tras reconstruir: aproximadamente 1,73 GB, por debajo del límite de 7 GB.

### Limitaciones
- El gate canónico continúa en FAIL por deuda clasificada en `079A-1`: 2 `broadcast-mutex` de diseño SSE, hallazgos de estructura/líneas/funciones largas y un parámetro excesivo; no se declara PASS ni se mezcla esta deuda con la meta web.
- Las pruebas Rust completas no se ejecutaron porque el guard del proyecto bloquea `cargo test` directo en este estado; sí se ejecutó la prueba funcional HTTP/SSE contra el artefacto real.

### Evidencia funcional final de meta web
- Artefacto ejecutado: `C:\tmp\glory-target\glory-harness\debug\glory-harness.exe`.
- SHA-256: `324288D19C8B643150EC78316A352F76FC4A106794159EB683C8D637A644B6E0`.
- Procedimiento: `web --puerto 8800 --fixture`, `healthz`, dos sesiones tokenless con cookies independientes y creación de conversación.
- Resultado: `PATCH /meta` normalizó `  objetivo claro  ` a `objetivo claro`; `modo=meta` produjo por SSE el token `[fixture] eco: [META: objetivo claro]\\nprueba meta`; se observaron `ready`, `turn.started`, `agent.event` y `turn.finished`.
- Limpieza: `meta: "   "` devolvió `null`; 8.001 caracteres devolvieron `413`; la segunda sesión intentando modificar la primera devolvió `401`.
- Corrección asociada: el fixture usa ahora `PreparacionTurno.mensaje_efectivo`, igual que el camino real, para que la prueba no oculte la aplicación de meta.

### Que se hizo
1. **Seleccionar elemento (frontend, solo UI):** boton "Seleccionar elemento"
   (icono `seleccionar`/mouse-pointer) en el panel navegador. Inyecta JS en la
   child webview via `navegador_js`: modo activo con outline 2px + crosshair;
   al hacer hover resalta el elemento; al hacer clic captura `{pagina: URL,
   selector CSS robusto (#id o tag+clases+nth-child), etiqueta corta, texto
   visible}` en `window.__ghSel__`. Polling lee el resultado y lo adjunta como
   badge (`adjunto-badge`) al chat activo. El siguiente mensaje antepone
   `[elemento de la pagina <url> — selector CSS: <sel> — etiqueta: <tag>]`.
   Modo web (iframe): solo aviso "requiere la app de escritorio (WebView2)".
   Archivos: panelNavegador.ts, entrada.ts, panelChat.ts, tipos.ts, iconos.ts,
   entrada.css, navegador.css, main.ts.
2. **Fix causa raiz (Rust):** el modelo NO veia `navegador_reflejo` al cambiar
   de modelo (p.ej. a deepseek-v4-flash-free). Causa: al cambiar modelo el
   front llama `reconfigurarSesion` (Tauri) → `SesionComun::reconfigurar()` →
   `reconstruir(opciones)` con `navegador: None` → el runtime nuevo no registra
   la tool (fail-closed). `SesionComun` ahora conserva el puerto
   (`pub navegador`) y `reconfigurar`/`cambiar_workspace` lo reinyectan.
   Archivo: cli/src/servicio/sesion.rs.

### Evidencia E2E seleccion (driver CDP, 9/9)
- Panel navegador abierto, child navega al fixture
  http://127.0.0.1:8797/fixture-sel.html via UI.
- Modo seleccion activo (`__ghSelActivo__` true); hover resalta `#btn-uno`
  (clase gh-sel-resaltado + crosshair).
- Click captura `{selector:"#btn-uno", etiqueta:"button#btn-uno.accion",
  texto:"Botón uno", pagina:"http://127.0.0.1:8797/fixture-sel.html"}`.
- Badge visible `elemento: button#btn-uno.accion · "Botón uno"`; boton vuelve
  a normal; mensaje enviado con el badge.
- Modo web: click "Seleccionar" loguea "requiere la app de escritorio".

### Evidencia E2E navegacion del modelo (con fix)
- Compilado: `C:\tmp\glory-target\glory-harness\debug\glory-harness-desktop.exe`,
  SHA256 8D42A38393060BB3098CFB4B710E334CDE9D797503ACB189C113BB1D2A862E8D.
- Modelo real (deepseek-v4-flash-free) tras el fix: LLAMO `navegador_reflejo`
  (visible en el chat como bloques "navegador_reflejo ... ok").
- Child webview NAVEGO a http://127.0.0.1:8797/fixture-sel.html y
  `window.__contadorClicks` = 1 (hizo clic en `#btn-uno`).
- Comparacion directa: la conversacion SIN fix respondio "no tengo herramienta
  de navegador interno" (navegador_reflejo ausente de su catalogo); la CON fix
  la uso. Confirmacion directa del fix.
- El turno fallo SOLO al final al generar la respuesta de sintesis: caida
  momentanea de TODOS los providers (deepseek key invalida ****shlo, commandcode
  503, glory en cooldown). NO es problema del fix: la tool ejecuto ok.

### Compilacion / gate
- cargo build desktop: OK (solo warnings ajenos pre-existentes: unused Arc en
  navegador.rs, leer_max_ventana dead code en main.rs).
- Gate: no ejecutado completo (bloqueado por deuda ajena pre-existente en
  main.rs/web.rs en refactor; ver notas del arbol).

### Archivos (esta sesion)
- cli/src/servicio/sesion.rs (fix: conservar puerto navegador)
- desktop/ui/src/componentes/panelNavegador.ts, entrada.ts, panelChat.ts,
  main.ts, tipos.ts, iconos.ts
- desktop/ui/src/estilos/entrada.css, navegador.css
- roadmap.md

## 069A-5 Deuda cero Sentinel (cerrada con transferencia, 07-09)

### Hecho en su día (06-09)
- F1 `94196f4` todo-pendiente ×4; F2 `51f2193` funcion-larga core+CLI;
  F3 `7cb4bc2` split `memoria.rs`; F4 `c782bc2` scheduler a
  `tareas_programadas/` (herramientas/ quedó en 10); F5 `f482a34`
  limite-lineas CLI. Core+CLI 0/0/0 por fichero entonces.
- F6 desktop nunca completada: bloqueada (main.rs ajeno, 28 errores).

### Cierre 07-09 (sin reclamar 0/0/0)
- Bloqueo levantado: `cargo check -p glory-harness-desktop` verde (solo 2
  warnings rustc preexistentes).
- Gate full 069A-5 re-ejecutado: FAIL 2E/10W/1I
  (`.quality-reports/check/069A-5/latest.md`). Persisten los 2 warnings F6
  (`enviar_turno` 134 en `main.rs:479`, `main.rs` 963→1177 líneas) y el
  código reciente reintrodujo deuda (herramientas/ otra vez 11 por
  `navegador.rs` de 069A-9; `web_datos.rs` 561 de 069A-2 F3; `red.rs`
  539 + `hojear_stream` 109; `despachar` 113; `ejecutar` 130).
- Deuda restante + nueva transferida al próximo plan (tabla entregada,
  plan aún no creado). Plan archivado en
  `Agente/planes/completados/plan-deuda-cero-sentinel-2026-09-06.md`.
- Preflight: staging sentinel desincronizado (`origin/main` en 902c45e) →
  `fetch <glory-sentinel> main:refs/remotes/origin/main` en
  `.quality-tools-harness/sentinel`; doctor verde después.


## 079A-1 Deuda cero Sentinel, segunda vuelta (CERRADA 0/0/0, 07-09)

### Hecho
- Guardian `8024e08`: `AgentToolContext` sin campo `navegador` en tests (roto
  en HEAD desde 069A-1 F5, 15 errores) + `PersistenciaMemoria` inexistente.
  Core `--lib` 15 errores -> 257 passed.
- F2 `cff9289`: `despachar`->opciones_run/despachar_run/despachar_chat,
  `ejecutar`->arg_str/op_script/op_dom/op_capturar,
  `hojear_stream`->extraer_evento_sse/fusionar_tool_call.
- F3 `a787146`: `web_datos.rs` 995->`web_datos/` por dominio;
  `hojear_stream`+SSE->`nucleo/llm/stream.rs`. CLI `--lib` 81 passed.
- F4 `16fadef`: `herramientas/navegador.rs`->`navegador/`
  (reflejo+operaciones+pruebas).
- F5 `922cf9a`: `enviar_turno`->`PaqueteTurno`+7 auxiliares,
  `abrir_sesion_interna` 9 params->`OpcionesApertura`, desktop `navegador.rs`
  756->`navegador/` (estado+comandos+webview2+puerto).
- F5b `baa7a2e`: desktop `main.rs` 1666->519
  (turno/sesion/conversaciones/workspaces/pruebas); `leer_max_ventana` a
  `pruebas.rs` (0 llamadas prod). Check desktop 0 warnings propios.
- F6 (glory-sentinel `1587c59` + repin `065b445` + marcas `#![cfg(test)]`
  `3db5f32`): unwrap/panic ignoran fichero solo-test (`rustTestScope.ts`,
  3 tests nuevos, budget 648/650). Gate 38E/2W -> 2E/2W -> 2E/0W.
- F1 `f5d9e69`+`caf4883`: fan-out SSE broadcast->mpsc por suscriptor
  (`web_sse.rs` `DifusionSse`; `SesionWeb.emitir`; evento `lagged`
  eliminado). CLI `--lib` 87 passed. Clippy 0 warnings propios.
- F7/F8 `ca8ff55`: auditorias SOLID y rendimiento (`Agente/documentacion/
  auditoria-{solid,rendimiento}-2026-09-07.md`), cero cambios de codigo.
- F9: gate full **PASS 0/0/0** (`.quality-reports/check/079A-1/`).
- Ajeno preservado: snapshot `541119e` + fix 1-char `eaf8c1e` (coma que
  rompia `tools_archivo.rs` en edicion concurrente).

### Decisiones/gotchas
- `leer_max_ventana` huerfana (P6 sin cablear): agendada, no tocada (F7).
- Scripts de corte con `pwsh` 7 (PS 5.1 lee ANSI y rompe asserts UTF-8).
- Comandos Tauri: `pub(crate) mod` + ruta completa (no re-export).
- E2E Tauri ventana real + `cargo test -p glory-harness-desktop` pendientes
  (disco C: lleno, decision usuario: no borrar).

### Evidencia
- Gate: `sentinel check 079A-1 --stages scripts/quality/stages.json --full`
  -> PASS 0/0/0. Doctor ready/readyForAnalyze/readyForGate true, 0 issues.
- Tests: core `--lib` 257 passed, CLI `--lib` 87 passed.
- Plan archivado en `Agente/planes/completados/plan-deuda-cero-079A-1-2026-09-07.md`.

## Post-F7: decisiones aplicadas (07-09)

- `leer_max_ventana` duplicada del desktop ELIMINADA (`d2aedf2`): la via
  real ya vive en `SesionComun::resolver_opciones` (lee
  `contexto_max_ventana`, default 150k, piso VENTANA_MINIMA) para todos los
  consumidores; los 3 tests se movieron a `cli/src/servicio/sesion.rs`.
  CLI `--lib` 90 passed, check desktop verde, gate 0/0/0.
- SQLite `spawn_blocking`+transaccion: NO aplicado. `persistencia_sqlite.rs`
  documenta decision deliberada del dueno (039A-3) en contra; imponerlo
  unilateralmente con agentes concurrentes seria regresion organizativa.
  Sigue como recomendacion a 039A-3.
- Split `tool.rs`: NO aplicado. Cohesivo (registro+politica), punto mas
  referenciado del nucleo, cero ganancia de gate; la separacion fisica real
  ya existe (tools concretas fuera).
- `hojear_stream` with_capacity: NO aplicado. Condicional a un caso que no
  existe (arguments >1 MB en miles de deltas); consta el invariante en F8.
