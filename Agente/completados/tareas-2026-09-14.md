# 2026-09-14 — cierre 139A-8 (F6n streaming/DOM + F7n pin del gate)

## 139A-8 F6n Streaming SSE/DOM eficiente (R5/R6/R8/R7, gate PASS, commit 050c426)
- Qué: R5 backend `cli/src/comandos/web/turnos.rs`: `CoalescedorEventos`
  puro (`nuevo/acumulado/ingesta/vaciar/por_techo`) + `reenviar_eventos` con
  `select!` + `interval(75 ms, Skip)` + `emitir_lote`; vacía-antes-viaja,
  techo 4 KB, `Done`/cierre vacían; canal 64, `DifusionSse` 256 `try_send`
  (`VENTANA_COALESCING_MS=75`, `MAX_TEXTO_ACUMULADO=4_000`). R6 backend:
  `titulo_actual()` (SELECT puntual `conversacion_obtener`, `None`=borrador);
  `turn.finished` ok/error/fixture con `{titulo,uso}`; fix borrow
  `*sesion.conversacion_id.lock().await`; `enviar_turno` mueve `mensaje`
  (fixture por `&preparacion.mensaje_efectivo`). R6 front: `CierreTurno` en
  `realTipos.ts` + `leerUltimoCierre/ultimoCierre` (`apiCliente.ts`, `api.ts`,
  `adaptadorReal.ts`); `panelChatTurno.ts:alTerminar` uso autoritativo +
  `ponerTitulo` optimista solo `ok` con extras, fallback `listar` clásico.
  R7 `panelChatHistorial.ts`: `pintarHistorial` autocontenida
  (`replaceChildren` + resetea mapa+cambios) + agrupación por bisección
  O(log U). R8 `sesion.rs:preparar_turno_con_modo` mueve `mensaje` en camino
  común (1 clon solo persistido en modo meta); resto auditado como inherente.
- Evidencia: `cargo test comandos::web::turnos` 18 passed (3 coalescing +
  `turno_fixture_finished_trae_titulo_y_uso`); harness DOM `C:\tmp\gh-f6n-r6`
  18/18; `tsc --noEmit` limpio salvo `src/main.ts` (hunks 139A-9 ajenos);
  `sentinel check 139A-8` **PASS**; commit `050c426` (9 ficheros, 139A-9
  excluido) + push `88e6261..050c426`.
- Gotchas: `-p glory-harness --lib` (no existe `glory-harness-cli`);
  `.bin/esbuild` no ejecutable en pipeline → `node .../esbuild`;
  `git archive | tar` falla en pipeline → `*.tar` + `tar -xf`.
- Prevención/Sentinel: sin cambios de conducta salvo `turn.finished` con
  título/uso y coalescing documentados.

## 139A-8 F7n Pin del gate + cierre (veredicto: SIN bump, tarea externa)
- Qué: auditoría §5.6 "este repo solo sube el pin" → verificar 10+5 reglas
  §4.4 upstream. **sentinel** (`1ndoryu/glory-sentinel`): pin local `1587c59`
  (v0.7.8) vs remoto `main=9f475d2`; delta 3 commits FP (039A-1, mocha 60s);
  `git grep` sobre `9f475d2`: **0/10 reglas** (solo preexistentes
  `funcion-larga-rs`/`limite-lineas-rs`). **varsense** (`1ndoryu/varsense`):
  remoto `main=88f281f` == pin; `src/rules/` sin reglas nuevas (**0/5**,
  solo `hardcodedDetection` genérico warning). Sin `gh` CLI ni checkout
  dueño en `area-trabajo` → tarea externa como spec lista-para-pegar (abajo).
- Decisión: NO bump (churn lock/setup sin beneficio + invalida la cadena de
  gates F6n sobre `1587c59`); plan §F7n actualizado con el veredicto.
- Evidencia: último `sentinel check 139A-8` **PASS** (12 archivos, 0 errores;
  warnings conocidos `directorio-abarrotado`/`funcion-larga-rs` en
  `contrato/ports.rs`); roadmap sin 139A-8; plan movido a
  `Agente/planes/completados/`.
- Pendiente real: NINGUNO en este repo (139A-8 cerrada). Externo: la tarea de
  abajo, propiedad de `glory-sentinel`/`varsense`.

## TAREA EXTERNA — 10+5 reglas §4.4 (propiedad: glory-sentinel + varsense)
- Estado: CREADA como spec (sin `gh` CLI; file manual por el dueño, mismo
  usuario). Spec completa en `Agente/documentacion/auditoria-integral-2026-09-13.md`
  §4.4 (líneas ~400-457): descripción, severidad sugerida y casos de este repo.
- Destino A `1ndoryu/glory-sentinel` (10 reglas): `rusqlite-bloqueante`,
  `shell-modelo-sin-guard`, `secreto-en-log`, `ruta-post-sin-jaula`,
  `path-join-sin-jaula`, `html-modelo-sin-sanitizar`, `sqlite-carga-sin-limite`,
  `clone-bajo-lock-async`, `god-object-rs`, `port-filesystem-duplicado`.
  Verificado ausentes en `main=9f475d2` (post-v0.7.9, solo fixes FP 039A-1).
- Destino B `1ndoryu/varsense` (5 reglas): `todo-prosa-observable`,
  `css-var-sin-token`, `orphan-plantilla-no-referenciada`,
  `ui-fanout-sin-broadcast`, `duplicado-cross-capa`. Verificado ausentes en
  `main=88f281f` (== pin local; `src/rules/` solo
  `naming/placeholders/hardcoded-detection` + suites framework/state-sync).
- Instrucción de file: 1 issue por repo con el título
  "Reglas de la auditoría glory-harness 139A-8 (§4.4)" + enlace a esta
  completada y a la auditoría §4.4; al publicar versión con las reglas,
  subir el pin en este repo (transacción §6 AGENTS.md: commit fuente,
  `quality:bump`, setup, lock, doctor, gate, sync guard).

## 149A-1 F4 Pin del gate 0.7.12 + 2.2.2 + cierre (cierra loop F7n/139A-8)
- Qué: releases dueño publicadas (sentinel `v0.7.11` 9 reglas Rust + 1 HTML +
  `v0.7.12` split mecánico modal; varsense `v2.2.2` F3.11/F3.13/F3.15 + opción
  F3.12) → pin en este repo: `quality-tools.json` sentinel `0.7.12/66a2113` +
  varsense `2.2.2/7dac28e`; staging con `fetch --tags` + `checkout --detach`.
- Desviaciones del plan: D1 `sentinel.lock.json` actualizado a mano
  (`generatedAt` + `version/commit`, `sha256` intactos, precedente `065b445`:
  `quality:setup` NO lo regenera, ningún script lo escribe); D2 pin real
  `0.7.12` (el plan decía `0.7.11`, bloqueada por `check-core` heredado).
- Evidencia: setup con evidencia escrita (sentinel `test:unit` 667 passing en
  staging, varsense compile+smoke); doctor listo (`ready/readyForGate` true,
  `issues: []`, `C:\tmp\doctor-0712.json`); analyze EXIT=1 esperado, 30
  findings (10E/15W/5H) en 21/272 archivos, determinista
  (`C:\tmp\analyze-0712-direct.json`).
- Triaje delta (detalle en plan §F4): 11 FP heurísticos + 2 TP-bajo
  solo-lectura (`content_search.rs:399`, `skill.rs:183`, sin fix en F4) + 1
  productor legítimo a allowlist (`mensajesUtil.ts:16`). Sin cambios de código
  (HEAD es WIP ajeno `f1dde81`). Gap H11-(b) derivado a `149A-2` (roadmap).
- Archivos: `quality-tools.json`, `sentinel.lock.json`, plan §F4, roadmap
  (149A-1 → [x], 149A-2 nueva). Sin bump de severidades globales.
- Prevención/Sentinel: `quality:setup` no regenera el lock aunque el plan lo
  afirme — el repin manual con precedente es el procedimiento real; proponer
  `quality:bump`/generador de lock (también pendiente en 139A-8 F7n).
