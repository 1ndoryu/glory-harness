# Tareas cerradas 2026-09-05 — Glory Harness

## 039A-3 P5: Gestor de 2 paneles (panel lateral máx 2, M1) — frontend

- Qué: frontend de P5 (2 conversaciones lado a lado, decisión M1: 1 runtime/turno global, turnos
  NO simultáneos). El backend P5 ya estaba hecho (commit `045f7e1`: estado por panel
  `Sesion.paneles`, comandos con `panel_id: Option<String>` default "principal", payloads de
  eventos SIN `panel_id`). Este bloque sustituye el `main.ts` monolítico (~1250 líneas) por un
  orquestador delgado (~706) + una fábrica `panelChat.ts` (~715) que encapsula un `.chat`
  completo duplicable. S3-b1: `real.ts` pasa `panel_id` por panel.
- Archivos:
  - `desktop/ui/src/componentes/panelChat.ts` (nuevo) — fábrica `montarPanelChat` → `PanelChat`
    (cabecera + mensajes + entrada duplicables, estado local, `activar`/`desenfocar`/`medir`/
    `empezarRenombrarCabecera`).
  - `desktop/ui/src/main.ts` (reescrito) — orquestador: sidebar + grip + `#paneles` + modal +
    `panelMeta` global M1; gestiona principal + lateral bajo demanda; turno global.
  - `desktop/ui/src/componentes/sidebar.ts` — acción "Abrir en panel lateral" (D4) en el menú
    contextual de cada fila (`puedeAbrirLateral()` + `onAbrirEnLateral(id)`).
  - `desktop/ui/src/estilos/layout.css` — media query `<900px` apila los 2 `.chat`.
  - `desktop/ui/src/tauri/real.ts` — `panelId?` en `OpcionesTurno`; `montar`/`detener` y
    `sesion.{nueva,cargar,eliminar,rewind,restaurarTramo}` pasan `panel_id` (S3-b1).
- Evidencia:
  - `npm run type-check` + `npm run build` (desktop/ui) limpios (dist 62.02 kB JS, 16.64 kB CSS).
  - Verificación funcional en navegador mock (`VITE_MOCK=1`, puerto 8762): (a) 2 paneles lado a
    lado con título correcto; (b) "Abrir en panel lateral" desde el ⋯ de fila de sidebar y desde
    el ⋯ de cabecera del principal; (c) al abrir lateral, la sidebar marca `.sel` su conversación
    y el lateral queda `.enfocado` (1 sola `.sel`); (d) al cerrar el lateral, el principal
    recupera `.enfocado` y la sidebar marca su conversación (fix `desenfocar`); (e) el lateral
    tiene entrada mínima (textarea+enviar, sin selector de modelo); el principal conserva el
    selector completo; (f) M1: enviar desde el lateral → ambos paneles "corriendo" (controles del
    principal `[disabled]` + botón "detener"); al detener/terminar ambos vuelven a "enviar" y se
    muestra "[turno cancelado por el usuario]" + pie de turno; (g) guard responsive: a 800px el
    menú ⋯ NO ofrece "Abrir en panel lateral"; (h) CSS: con 2 chats a 800px `#paneles` usa
    `flex-direction: column` (principal y=0 h=624, lateral y=624 h=179).
  - Capturas: `C:/tmp/glory-harness-s3b-2paneles.png`, `C:/tmp/glory-harness-s3b-2paneles-final.png`,
    `C:/tmp/glory-harness-s3b-responsive-800.png`.
- Decisiones/gotchas:
  - Se eliminó `onRenombrar` de `DepsPanel`: el ⋯ de cabecera delega vía
    `panel.empezarRenombrarCabecera(...)` y el orquestador conecta `renombrarEnLista`.
  - Bug de foco al cerrar el lateral (0 paneles `.enfocado`): fix con método público
    `desenfocar()` en `PanelChat`; `activarPanel` lo aplica sobre los demás.
  - El selector de modelo del principal necesitaba los proveedores: se añadió
    `proveedores?: ProveedorModelo[]` a `PanelChatOpciones` y `modeloActual: d.getModelo()` al
    `montarEntrada`; el orquestador pasa `PROVEEDORES` al crear el principal.
  - Backend P5 (045f7e1) y front P5 se desarrollan en paralelo con otro agente (059A, 318A-17)
    que toca `cli/`/`core/`; los cambios ajenos se respetaron (nunca `git add .`).
- Pendiente real: E2E `tauri dev` (2 conversaciones reales, turnos seriales, eventos al panel
  correcto) — pendiente del plan P5, bloqueada mientras el agente paralelo toca `cli/`/`core/`.

## 039A-3 P6-front: Indicador circular de contexto + config ventana 150k

- Qué: frontend de P6 (el backend — inyección de `contexto.max_ventana` en `cli/run.rs` — queda
  PENDIENTE por colisión con el agente paralelo en `cli/`/`core/`). El indicador circular junto
  al botón enviar muestra el % de ocupación de contexto; la opción "Ventana de contexto"
  (default 150000) se añade a Configuración → Contexto y se persiste en config.
- Archivos:
  - `desktop/ui/src/componentes/entrada.ts` — SVG circular `.ctx-indicador` (`.ctx-pista` tenue +
    `.ctx-lleno` con `stroke-dasharray`/`stroke-dashoffset` según %) antes de `.btn-enviar`;
    método `setContexto(pct, maxVentana)` en la API `Entrada`; `pintarContexto` clampea 0-100 y
    rotula tooltip (`contexto N%` o `contexto · hasta Nk` sin dato).
  - `desktop/ui/src/estilos/entrada.css` — `.ctx-indicador` (flex:none, `margin-left:auto` para
    anclar indicador+enviar a la derecha en ambas variantes), `.ctx-pista` (stroke tenue),
    `.ctx-lleno` (stroke negro, sin relleno, `stroke-linecap: round`).
  - `desktop/ui/src/componentes/panelChat.ts` — `setContexto(pct, maxVentana)` en la interfaz y
    la API `PanelChat` (delega en `entrada.setContexto`); en la rama mock de `alTerminar` llama
    `entrada.setContexto(7, 150000)` (espejo del pie mock, para verificación visual).
  - `desktop/ui/src/tauri/real.ts` — hook opcional `onContexto?(uso: UsoTurno)` en
    `HooksAdaptador` (no rompe API), invocado en `aplicar()` al procesar `usage` y
    `contexto_detalle` (fuente única del %: el `ContextoDetalle`).
  - `desktop/ui/src/main.ts` — en `crearAdaptadorReal` pasa `onContexto(u)` que repinta
    `panelesRegistrados.forEach(p => p.setContexto(u.ocupacionPct, u.maxVentana))`; en el modal
    `onCambio` persiste `contexto_max_ventana` vía `configGuardar` (real); en el arranque real lee
    `configLeer('contexto_max_ventana')` y `modal.asignarValor`.
  - `desktop/ui/src/dominio/opciones.ts` — opción `contexto_max_ventana` ("Ventana de contexto",
    tipo `texto`, valor `150000`) en `OPCIONES_CONTEXTO`.
- Evidencia:
  - `npm run type-check` + `npm run build` (desktop/ui) limpios (dist 63.68 kB JS, 17.09 kB CSS).
  - Verificación funcional en navegador mock (`VITE_MOCK=1`, puerto 8765): (a) el indicador
    circular aparece antes de `.btn-enviar` en la variante completa (principal) y en la mínima
    (lateral); (b) tras un turno mock, el círculo muestra `contexto 7%` (dashoffset 52.59 =
    56.55×(1−0.07)) y el pie `1k → 385 · glory/gpt-4.1 · 9k/150k (7%)`; (c) el `margin-left:auto`
    ancla indicador+enviar a la derecha en ambas variantes (completa: 119px auto; mínima: 409px
    auto); (d) Configuración → Contexto muestra "Ventana de contexto" con valor 150000 editable.
  - Capturas: `C:/tmp/glory-harness-p6-ctx-7pct.png`, `C:/tmp/glory-harness-p6-config-ventana.png`,
    `C:/tmp/glory-harness-p6-lateral.png`.
- Decisiones/gotchas:
  - Fuente única del % (decisión §2.10): pie e indicador usan SIEMPRE `ocupacion_pct` y
    `max_ventana` del `ContextoDetalle`/`UsoTurno`; nunca se duplica cálculo.
  - En el mock no hay persistencia a localStorage para estas opciones (igual que `modo`/
    `nivelRazonamiento`, que solo persisten vía `configGuardar` en modo real); el control se
    edita y en modo real el cambio se persiste.
  - En real mode hoy `max_ventana` será 128k (default core) hasta que el backend P6 inyecte la
    ventana configurada — documentado como limitación en el plan.
  - Se respetaron los cambios ajenos del agente paralelo (`cli/`/`core/` y untracked de
    `desktop/src-tauri/`); `git add` explícito solo de los 6 archivos del front.
- Commit: `769492d` — "039A-3 (P6 front): indicador circular de contexto + opcion ventana 150k".
- Pendiente real: backend P6 (inyectar `contexto.max_ventana` configurado en `construir_harness_con`/
  `reconfigurar_sesion` de `cli/run.rs`, sin tocar el default del core 128k) — bloqueado por el
  agente paralelo; E2E `tauri dev` para confirmar que 150k llega como `ContextoDetalle.max_ventana`.

## 039A-3 P6-front (+): Menú hover de detalle del uso de la ventana de contexto

- Qué: requisito del usuario ("hace falta un pequeño menú contextual cuando se ponga el cursor
  sobre el círculo que diga detalladamente el uso de la ventana de contexto"). Al hacer hover o
  foco sobre el `.ctx-indicador` (círculo de contexto) se abre un pequeño menú informativo
  `.ctx-detalle` con el detalle del uso de la ventana; se cierra al salir.
- Archivos:
  - `desktop/ui/src/componentes/entrada.ts` — interfaz exportada `EstadoContexto` (`pct`,
    `maxVentana`, `reservaSalida`, `totalEntrada`); `setContexto(estado)` guarda el estado
    completo y pinta el arco; menú `.ctx-detalle` (`abrirDetalle`/`cerrarDetalle`) con
    `mouseenter`/`focus` abre y `mouseleave`/`blur`/click fuera/Escape/resize/scroll/blur cierra;
    posicionado con el vuelco de `menu.ts` (margen 8, bajo el rect, invierte si no cabe); contenido
    `filasDetalleContexto` (usados/reserva/entrada con números exactos `toLocaleString('es')` y
    separador de miles; "sin datos" si no hay turno, "configurada N" si solo hay `max_ventana`).
    Se elimina el `title` nativo del círculo (lo sustituye este menú custom).
  - `desktop/ui/src/componentes/panelChat.ts` — `setContexto(estado: EstadoContexto)` en la
    interfaz y la API `PanelChat` (delega en `entrada.setContexto`); en la rama mock de
    `alTerminar` pasa `{pct:7, maxVentana:150000, reservaSalida:20000, totalEntrada:9100}`
    (espejo del pie mock, para verificación visual).
  - `desktop/ui/src/main.ts` — `onContexto(u)` propaga el `EstadoContexto` completo (pct +
    maxVentana + reservaSalida + totalEntrada) a `panelesRegistrados`.
  - `desktop/ui/src/estilos/entrada.css` — `.ctx-detalle` (position:fixed, z-index 110 por encima
    de `.menu-ctx`, `pointer-events:none`, monocromo, white-space:nowrap), `.ctx-detalle-titulo`
    (opacity .6, border-bottom), `.ctx-detalle-fila` (flex etiqueta/valor).
- Evidencia:
  - `npm run type-check` + `npm run build` (desktop/ui) limpios (dist 66.08 kB JS, 17.53 kB CSS).
  - Verificación funcional en navegador mock (`VITE_MOCK=1`, puerto 8765): (a) hover inicial →
    "ventana de contexto / uso / sin datos"; (b) tras turno mock → "usados 9100 de 150.000 (7%) /
    reserva de salida 20.000 / entrada del turno 9100" (círculo `contexto 7%`, dashoffset 52.59);
    (c) al salir del círculo el detalle se cierra (0 nodos `.ctx-detalle`) y al re-entrar se reabre;
    (d) el panel lateral (entrada mínima, 2º `.ctx-indicador`) también muestra el detalle con el
    mismo estado "sin datos".
  - Capturas: `C:/tmp/glory-harness-p6-hover-detalle.png`,
    `C:/tmp/glory-harness-p6-hover-lateral.png`.
- Decisiones/gotchas:
  - El detalle es INFORMATIVO (sin ítems accionables): `pointer-events:none` para que lo abra y
    cierre el hover del propio círculo y nunca bloquee otros menús; `z-index` 110 por encima de
    `.menu-ctx`.
  - No se reutilizó `menu.ts` (`.menu-ctx` se cierra por click fuera y tiene `pointer-events:auto`);
    el detalle es un componente propio porque su ciclo de vida es hover (abrir/cerrar por
    mouseenter/mouseleave), no click.
  - Fuente única de datos = el mismo `ContextoDetalle`/`UsoTurno` del indicador (§2.10); se
    sustituye el `title` nativo del navegador por este menú custom (coherente con el detalle y la
    estética).
  - Números EXACTOS con separador de miles (`toLocaleString('es')`) en el detalle (no
    `tokensCortos` con sufijo k) para que el hover diga el uso con precisión; el pie conserva el
    formato corto.
  - Se respetaron los cambios ajenos del agente paralelo (`cli/src/ui/tui/*` y untracked de
    `desktop/src-tauri/`); `git add` explícito solo de los 4 archivos del front.
- Commit: `a69c2a3` — "039A-3 (P6 front): menú hover de detalle del uso de la ventana de contexto".
- Pendiente real: el mismo que P6-front base (backend P6 bloqueado por el agente paralelo;
  E2E `tauri dev`).

## 039A-3 P6b: Mejoras UX de los paneles (redimensionable + controles compartidos + ⋯ a la derecha + lista auto-ocultable)

- Qué: requisitos del usuario (05-09) sobre la experiencia de paneles: (1) poder cambiar el
  tamaño del panel lateral con un divisor arrastrable; (2) el panel lateral debe ofrecer también
  elegir modelo/razonamiento/modo y escribir mensaje (antes era una entrada mínima); (3) el ⋯
  de la cabecera va al extremo derecho; la lista de conversaciones NO se oculta manualmente
  (sin botón de ocultar): se auto-oculta al reducir la ventana bajo un ancho mínimo y el botón
  de la cabecera solo aparece cuando está oculta, para MOSTRARLA.
- Archivos:
  - `desktop/ui/src/main.ts` — divisor vertical arrastrable `.lateral-grip` entre principal y
    lateral (mousedown → mousemove con clamp `[260, round(ancho×0.7)]` → mouseup persiste
    `lateral_ancho` en localStorage); `restaurarLateralAncho()` aplica el persistido (default
    mitad de `#paneles`) al reabrir el lateral; `crearGripLateral`/`cerrarLateral` insertan y
    retiran el grip; `aplicarLateralAncho(px)` fija `--lateral-ancho`. Sidebar auto: umbral
    `720px` + `sidebarForzada`; `aplicarSidebar()` oculta la lista sola por debajo del umbral y
    `mostrarSidebar()` la fuerza visible (guarda `sidebar_colapsada='0'`); listener `resize`.
    Handlers de modelo/modo/razonamiento: ahora propagan M1 a `panelesRegistrados` (forEach
    `setModelo`/`setModo`/`setRazonamiento`); `proveedores: PROVEEDORES` para ambos tipos.
  - `desktop/ui/src/componentes/panelChat.ts` — `montarEntrada` usa `variante: 'completa'` para
    TODOS los paneles (antes el lateral usaba `'minima'`); comentarios de interfaz actualizados.
  - `desktop/ui/src/componentes/cabecera.ts` — el botón ⋯ se monta en un grupo propio
    `acciones-mas` al EXTREMO DERECHO (tras el título, `margin-left:auto`); el toggle/× queda a
    la izquierda. `btnToggle` nace `hidden`, solo aparece con lista oculta y su única acción es
    MOSTRAR (`onToggleSidebar`); `setSidebarAbierta(abierta)` fija `btnToggle.hidden = abierta`.
  - `desktop/ui/src/estilos/layout.css` — `#paneles` con `--lateral-ancho: 50%`; el borde y el
    ancho variable pasan a `.chat[data-panel='lateral']` (`flex: 0 0 var(--lateral-ancho)`; ya no
    vale `.chat + .chat` porque lo rompe el grip); `.lateral-grip` (5px, `col-resize`),
    `body.redimensionando-lateral`; `.acciones-mas` con `margin-left:auto`;
    `.cab-boton[hidden]{display:none}` (anula el `inline-flex`); media `<900px`: columna, sin
    grip, lateral `flex:1 1 auto`.
- Evidencia:
  - `npm run type-check` + `npm run build` (desktop/ui) limpios.
  - Verificación funcional en navegador mock (`VITE_MOCK=1`, puerto 8765): (a) abrir lateral
    desde ⋯ → lateral con cabecera (× izq, título, ⋯ der) y entrada completa (Laguna S 2.1 Free /
    Medio / Predeterminado / contexto / enviar); (b) anchos: principal flex 1, lateral
    `flex: 0 0 460px` (≈50% de 919), grip 5px; (c) arrastre del grip → 306px; arrastre extremo →
    clamp 260px con `localStorage.lateral_ancho='260'`; (d) cerrar lateral (×) → grip eliminado y
    principal a ancho completo; (e) reabrir lateral → restaura 260px persistido; (f) viewport
    650px (<720) → `sidebar-colapsada` activa, botón "mostrar lista de conversaciones" visible;
    (g) clic en ese botón → lista forzada visible a 650px y botón se oculta; (h) viewport 1200px →
    lista visible y botón oculto; (i) viewport 650px con 2 paneles → columna (chats 650px) y grip
    oculto; (j) posiciones ⋯: principal x=601 (de 641), lateral x=219 (de 259) → extremo derecho;
    (k) cambio de modelo en el LATERAL a "DeepSeek V4 Flash" → propagado al principal (ambos
    muestran DeepSeek V4 Flash) → M1 compartido OK.
  - Capturas: `C:/tmp/p6b-2paneles.png`, `C:/tmp/p6b-angosto.png`.
- Decisiones/gotchas:
  - Requisito del usuario: la lista NO se oculta manualmente. `CLAVE_COLAPSADA` ya no se usa para
    ocultar (se guarda '0' en `mostrarSidebar`); se conserva la lectura inicial por compatibilidad
    (si estaba colapsada, aparece el botón para mostrarla). Coherente con P6b.
  - `.cab-boton` usa `display:inline-flex` que anula `hidden` → regla
    `.cabecera-chat .cab-boton[hidden]{display:none}`.
  - `.chat + .chat` (border-left) lo rompe el grip insertado → el borde se mueve al lateral
    `[data-panel='lateral']`.
  - En `page.evaluate` no hay `setTimeout`; para re-validar tras resize se dispara
    `window.dispatchEvent(new Event('resize'))` y para cambiar viewport real se usa
    `page.setViewportSize` de Playwright (`window.resizeTo` no cambia innerWidth).
  - Se respetaron los cambios ajenos (untracked de `desktop/src-tauri/`); `git add` explícito solo
    de los 4 archivos del front.
- Commit: `67729b5` — "039A-3 (P6b): paneles redimensionables, controles compartidos y lista auto-ocultable".
- Pendiente real: E2E `tauri dev` (mismo bloqueo del agente paralelo en `cli/`/`core/`).

## 039A-3 P6b retoque: colapso por arrastre del grip + sin hueco del botón oculto

- Qué: dos correcciones del usuario tras probar P6b (05-09): (1) al contraer la lista
  arrastrando el divisor de la sidebar hacia la izquierda, NUNCA se ocultaba (el clamp
  `[180,420]` la dejaba clavada en 180px); (2) cuando el botón "mostrar lista" está oculto
  (lista visible), su grupo contenedor seguía en el DOM y dejaba un hueco de ~16px (gap del
  `.cabecera-chat`) entre el borde izquierdo de la cabecera y el título.
- Archivos:
  - `desktop/ui/src/main.ts` — drag del grip de la sidebar: en vivo el ancho se puede encoger
    hasta `UMBRAL_COLAPSO=100` (antes `MIN=180`); al soltar, si el ancho quedó ≤ umbral la
    lista COLAPSA del todo (`sidebarAbierta=false`, `aplicarSidebar()`, guarda
    `sidebar_colapsada='1'` y rearma `--sidebar-ancho` a `ANCHO_REABRIR=260`); si no, aplica
    el clamp `[MIN,MAX]` como antes y guarda `sidebar_colapsada='0'`. El colapso se decide al
    soltar (no en vivo) para no perder el grip a mitad de gesto.
  - `desktop/ui/src/estilos/layout.css` — regla
    `.cabecera-chat .acciones-cabecera:has(.cab-boton[hidden]) { display:none }`: el grupo
    izquierdo colapsa cuando su botón "mostrar lista" está oculto. No afecta al lateral (su
    grupo contiene el ×, nunca un botón con `[hidden]`).
- Evidencia:
  - `npm run type-check` + `npm run build` (desktop/ui) limpios.
  - Verificación funcional en navegador mock (`VITE_MOCK=1`, puerto 8765): (a) con lista
    visible, el grupo izquierdo de la cabecera mide 0px y `display:none` → el hueco entre el
    borde de la cabecera y el título pasó de 32px a 16px (solo el padding del `.cabecera-chat`);
    (b) arrastre del grip hasta el borde (clientX en el left de `#cuerpo`) → sidebar
    `display:none` + clase `sidebar-colapsada`, `localStorage.sidebar_colapsada='1'` y
    `sidebar_ancho='260'`; el botón "mostrar lista de conversaciones" aparece en la cabecera;
    (c) clic en ese botón → la lista reaparece (`flex`, `sidebar_colapsada='0'`) y el botón se
    oculta; (d) arrastre del grip a 300px → NO colapsa, queda en 300px persistido; (e) el
    panel lateral no se ve afectado: su grupo izquierdo sigue `flex` con el × visible (30px).
  - Capturas: `C:/tmp/p6b-ret-boton-oculto-sin-hueco.png`, `C:/tmp/p6b-ret-colapsado-boton.png`.
- Decisiones/gotchas:
  - El colapso total al arrastrar hasta el borde es la forma de cerrar la lista (el usuario
    pidió que "contraer" la oculte del todo); no hay botón manual de ocultar, solo el gesto
    de arrastre y el botón "mostrar lista" para reabrir.
  - El colapso se decide en `mouseup` (no en vivo) para conservar el grip durante el gesto.
  - Se usó `:has()` (soportado por WebView2/Chromium de Tauri 2 y navegadores modernos);
    alternativa sin `:has` sería ocultar el grupo por clase desde JS.
  - Se respetaron los cambios ajenos (agente paralelo en `core/src/nucleo/*` y untracked de
    `desktop/src-tauri/`); `git add` explícito solo de los 2 archivos del front.
- Commit: `aa8dbd4` — "039A-3 (P6b retoque): colapsa la lista arrastrando el grip + sin hueco del boton oculto".
- Pendiente real: E2E `tauri dev` (mismo bloqueo del agente paralelo en `cli/`/`core/`).

## 059A-1: Plan de saneamiento de calidad (S0–S8) — cerrado

- **Plan:** `Agente/planes/plan-saneamiento-calidad-2026-09-05.md` · **ID:** 059A-1 · fecha 05-09.
- **S0:** baseline congelado (0 err / 21 warn / 10 archivos, glory-sentinel 0.7.7);
  snapshots en `Agente/completados/analyze-baseline-2026-09-05.json`.
- **S1 (falsos positivos del gate):** corregido en glory-sentinel `902c45e` v0.7.8
  (`directorio-abarrotado` ignora lock/manifests/config de raíz; doble reporte
  `limite-lineas`+`-nivel-2` documentado intencional). Repineado lock.
- **S7 (brechas del gate):** 3 reglas nuevas implementadas con tests en glory-sentinel
  (`block-en-async-rs` error, `lock-a-traves-await-rs` warning, `expect-produccion-rs`
  error); propuestas restantes en `Agente/documentacion/brechas-gate-2026-09-05.md`.
- **S2 (splits estructurales):** `f33a4de` (runtime.rs→dir), `3910477` (llm.rs→dir),
  `f3d467b` (cli tui.rs→dir). Cero cambio de comportamiento.
- **S3 (funciones largas):** `79022c7` — diff/main/chat/red/subagente/turno/TUI;
  `funcion-larga-rs`/`limite-lineas-rs` a 0 en core+cli.
- **S4 (directorios por dominio):** `8064c41` — core/src→`nucleo|herramientas|politica|
  contrato`, cli/src→`comandos|ui|infra` con re-exports planos (git mv).
- **S5 (auditoría SOLID):** `60a0ade` — informe `Agente/documentacion/auditoria-solid-2026-09-05.md`.
- **S6 (auditoría de rendimiento):** `ce5af4d` — fix TUI 4151→60 µs/frame (~70×) en
  streaming, 22 µs/frame idle (caché + ventana visible); informe
  `auditoria-rendimiento-2026-09-05.md`. Hallazgo SQLite (ajeno 039A-3) documentado.
- **Gate final:** `cargo test --workspace` 234 verdes (195 core + 35 cli + 4 desktop),
  clippy core+cli `-D warnings` limpio, `quality:analyze` **0 hallazgos en core+cli**
  (quedan solo 3 ajenos: persistencia_sqlite.rs, desktop main.rs/vault.rs), doctor
  `ready: true`/`readyForGate: true`; PT `cargo check` report-only = 2 errores
  preexistentes `missing field web_fetch` (B3-F1, independientes del layout).
- **Pendientes reales:** ajenos de 039A-3 (persistencia_sqlite.rs + desktop) con
  recomendación SQLite de S6; propuestas de reglas gate fuera de D4 (brechas-gate).
