# 2026-09-21 - cierre 219A-1 (canon botón icono + títulos chrome)

## 219A-1 Botones icono y títulos centralizados (canon 28×28 sin borde)
- Qué: F1 `desktop/ui/src/componentes/chromePanel.ts` (`crearBotonIcono({icono,
  etiqueta, alPulsar?, activo?, invertidoHover?, deshabilitado?, id?, claseExtra?})`
  + `crearCabeceraPanel({claseRaiz, titulo, medio?, acciones?})`) y
  `desktop/ui/src/estilos/botonIcono.css` (canon: 28×28 sin borde, `.ic` 16px,
  hover `superficie-hover`, active `superficie-active`, focus outline 2px `foco`,
  disabled opacity .35, `.activo` invertido permanente, `.invertido-hover` para
  ⋯/× del chat, `[hidden]{display:none}`, `.chrome-titulo` `--sm`). F2 migración
  de los 6 sitios (barraSuperior, Consola, Files, Navegador, tabs `+`,
  cabecera ⋯/×) + borrado de `.barra-boton/.cab-boton/.tab-mas/.consola-accion/
  .files-accion/.nav-btn`; títulos a `--sm` (Consola `--md`→`--sm`).
- Cambios intencionales que NO son regresión: Consola 24→28px; Files pierde
  borde; Nav pierde borde-hover e iconos 11px→16px; tabs `+` pierde
  `opacity:.65` idle; cabecera ⋯/× 24→28px; `cerrarCaptura` Nav pasa a `.btn`
  texto (mismo affordance que ⋯/× del chat, 039A-3); `abrirCon` normalizado
  `…`; `alternarLista` gana `title`/`aria-label`/`aria-expanded`.
- Incluye lote visual Consola del 20-09 (estaba sin commit: `:disabled`,
  `:focus-visible`, `.consola-lista:empty{display:none}`, `hidden` vs
  `style.display` + reglas `[hidden]`) + mejoras Files integradas en el árbol
  (grip arrastrable `seguirPuntero`, revelar-en-árbol `normalizarRuta`/
  `cargarRaiz`, marca `.seleccionado`, cableado `pintarBotonLista` con clase
  `files-sin-explorador`): el botón `alternarLista` migrado solo funciona con
  ese cableado, por eso viajan en el mismo commit en vez de partir el fichero.
- Evidencia: `npm run type-check` EXIT 0; `npm run build` OK (`✓ built in
  3.65s`); VarSense `scan --workspace . --format json` 15E/14W idéntico al
  baseline, 0 nuevos (preexistentes en zona: `--files-nivel` con fallback +
  `setProperty` del grip, `--sidebar-ancho` en `#cuerpo`, `todoProsa` =
  "todo" dentro de "método" en comentarios 089A-3, `rgba` tema-synara);
  `Select-String` clases viejas en `desktop/ui/src` = 0 salvo comentarios;
  **DOM en vivo** (vite dev `:8760` + snapshot + `getComputedStyle`):
  18/18 `.boton-icono` 28×28/`border none`/`.ic` 16px (barra, tabs, cabecera,
  Consola, Files + 6 del Navegador tras abrir su tab), título Consola
  11.05px (= `--sm`), 4 deshabilitados a opacity 0.35, 0 clases viejas en DOM.
- Archivos: `componentes/chromePanel.ts` + `estilos/botonIcono.css` (nuevos);
  `barraSuperior.ts`/`.css`, `panelConsola.ts`/`consola.css`,
  `panelFiles.ts`/`files.css`, `panelNavegadorControles.ts`/`navegador.css`,
  `panelDerecho.ts`/`tabs.css`, `cabecera.ts`/`layout.css`, `tema.css`,
  `tema-synara.css`; spec en `Agente/documentacion/
  ui-chrome-botones-titulos-2026-09-21.md`; plan en
  `Agente/planes/completados/plan-219A-1-botones-cabecera-2026-09-21.md`.
- Prevención/Sentinel: VarSense SÍ corre dentro del gate (`sentinel analyze`,
  etapa `sentinel` en `scripts/quality/stages.json`,
  `analyzers.varsense.enabled: true`), pero sus reglas son léxicas — la
  coherencia visual entre ficheros (un botón por panel con medidas distintas)
  sigue sin ningún check automático. Decisión: NO añadir regla que prohíba
  clases de botón por panel (falsos positivos con hooks posicionales
  legítimos como `.files-visor-lista`); el spec documenta el canon y el
  qué-no-usar. Gate completo cargo no ejecutado (cambio solo-UI;
  type-check+build+VarSense+DOM en vivo como evidencia proporcional).
- Pendientes reales: ninguno de 219A-1. En el árbol queda WIP ajeno sin
  commit (Rust: daemon/run/web/persistencia/sesión/turno/contexto/llm/
  runtime; UI: mensajes*.ts, panelChat*.ts, modal, api; `.gitignore`;
  `plan-209A-1-...md`) — NO tocado por este commit.

## 219A-2 Botón + tabs: centrado vertical
- Qué: el `+` quedaba pegado arriba de la barra (`align-items: stretch` para
  las tabs + altura fija 28px del canon = `flex-start` efectivo; antes, sin
  altura propia, estiraba y centraba su icono). Fix de una línea en
  `desktop/ui/src/estilos/tabs.css`: `.tabs-barra > .boton-icono
  { align-self: center }` (posicional, solo el botón).
- Evidencia: DOM en vivo (vite dev `:8760`, harness `.tabs-barra` + `.tab`
  con padding 14px): barra 41px, botón top 6px = centrado (`(41-28)/2`);
  con `align-self: flex-start` forzado top 0 = bug reproducido; regla
  confirmada en `document.styleSheets` en vivo. Nota: con la altura real
  (~29px) el desfase es 0.5px, invisible a `offsetTop` entero — por eso el
  harness usa tab alta.

## 219A-3 Tab Consola como visor interactivo
- Qué: la tab deja de explicar y pasa a operar. F1 contrato
  (`TranscriptConsola{id_ejecucion,comando,viva,codigo_salida,lineas}` +
  `escribir(id,bytes)->usize` tope 64 KB + `salida(id)->Transcript` tope
  2000 líneas, defaults `NoEncontrado`) en `core/src/contrato/ports.rs`.
  F2 ejecutor: `stdin(piped)` + `take()` en `ejecutar_fondo_con_id`, anillo
  `VecDeque<ChunkConsola>` (con flujo), `escribir` (`write_all+flush`,
  BrokenPipe→`NoEncontrado`) + `salida` (viva=anillo, archivada=merged
  stdout) + test `escribir_acepta_en_viva_y_falla_tras_matar`. F3 web:
  `GET /consolas`, `GET /consolas/:eid/salida`, `POST
  /consolas/:eid/escribir {texto}` (`{ok,escritos}`; vacío/>64KB→
  `peticion_invalida`, desconocida→`no_encontrado` NO idempotente). F4
  Tauri: `consolas_listar`/`consola_salida`/`consola_escribir` (+ structs).
  F5 transporte: tipos + métodos en `realTipos.ts`, `transporteTauri.ts`,
  `api.ts`, `adaptadorReal.ts`. F6 panel: sub-barra interna (ver +
  seleccionar), `sincronizar()` al abrir (backfill sin robar selección ni
  duplicar vivo), `cargarTranscript` + `pendientes` (chunks durante la
  carga), `acotar` (1000 líneas vista), stdin Enter (`texto+\n`, crudo sin
  eco), frame "Consola lista..." en vacío, nota stdin honesta; comando de
  entrada rescatada lo corrige `sincronizar` (backend sabe el verbatim).
  Sin PTY; sin lanzar comandos desde la UI.
- Incluye fontanería mínima del resto F4 (sin ella no compila: mis handlers
  usan `comun.ejecutor`): campo `SesionComun.ejecutor` + adopción al abrir
  y al reconstruir en `cli/src/servicio/sesion.rs` (4 hunks, solo eso). El
  resto F4 (reap al eliminar/cerrar, ruta+comando `matar`, reap Tauri/Ctrl+C)
  y el WIP ajeno (resume sesión cookie, `cambiosListar`, `UsoTurno*`,
  refactors daemon/run/llm/runtime/mensajes/sidebar) quedan SIN commitear —
  stage por hunk verificado.
- Evidencia: `sentinel check 219A-3 --stages scripts/quality/stages.json`
  → PASS (coverage/sccache/sentinel cached, rust 541 ok 0 fallidos;
  fast rechazado con `rust-pide-completo` por tocar `src-tauri`);
  `npm run type-check` EXIT 0; `npm run build` OK; funcional en vivo (`web
  :8799` binario fresco: `GET /consolas` → `{ok:true,consolas:[]}`,
  salida/escribir desconocida → `no_encontrado`). Positivo vivo no
  disparable por API (cubierto por test Rust). Incidencia: proceso
  `glory-harness` colgado (PID 16092, 20-09) bloqueaba el link del gate
  (`Acceso denegado` en `debug\glory-harness.exe`) — detenido antes del PASS.
- Desvíos del plan: comandos `consolas_listar` (plural); `revelar()` también
  pide backfill; sin clase `.consola-lista` nueva (se reutiliza la lista).
- Plan en `Agente/planes/completados/plan-219A-3-consola-interactiva-2026-09-21.md`.
- Pendientes reales: stdin con programas que no leen de tubería (eco
  inexistente: honesto en nota); `funcion-larga` en `ejecutar_fondo_con_id`
  (129) y `comando.rs:ejecutar` (115) preexistentes/ajenos al stage.
- CIERRE 21-09 por decisión del usuario: commit de TODO el árbol (219A-3 +
  trabajo olvidado: resume por cookie, frente Cambios, turnos, navegador por
  agente, resto F4 `matar`, refactors daemon/run/llm/runtime/mensajes/
  sidebar, scripts desktop, `.gitignore`). `type-check` verde + gate PASS
  previo (541 tests) sobre el mismo código. Basura `prueba.txt`/
  `prueba-2.txt` borrada; `.glory-harness/backups/` excluido (runtime).
  El intento de stage quirúrgico previo queda como lección (ver abajo).
  Previo (lección): intento de stage quirúrgico con script `stage_219a3.py`
  (11 ficheros filtrados con aserciones + 7 íntegros). Hallazgos: (1) bug
  propio detectado por la verificación: `git apply` coloca inserciones
  puras según `+start` y el script lo preservaba sin recalcular → código
  8–62 líneas desplazado (corregido: `new_s = old_s + delta_prev + 1`);
  (2) HEAD no compilaba sola — `main.ts` (HEAD) no satisfacía
  `GanchosDeps/DepsCrearPanelCtx/NavegadorVistaDeps`, el cableado vivía en
  WIP 179A-2/129A-8 → frente Cambios; (3) Rust igual: `harness.
  ejecutor_comando` no existía en HEAD (lo definía el WIP de `run.rs`/
  `daemon.rs`). Un commit solo-219A-3 habría sido rojo; por eso el usuario
  ordenó commitear todo. Lección: verificar el subset con ejecución real
  (`stash --keep-index` + checks) ANTES de cantar PASS estático; y nunca
  `git reset` antes de `git stash pop`.
